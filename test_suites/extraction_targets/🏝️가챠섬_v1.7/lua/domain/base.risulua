local M = {}
local __impl = {}

-- " xN" suffix 제거 (스택 아이템 호환)
function __impl.eqExtractBase(rawName)
    if not rawName then return "" end
    local base = rawName:match("^(.-)%s+x%d+$") or rawName
    return base:match("^%s*(.-)%s*$") or base
end

M.eqExtractBase = __impl.eqExtractBase

return M
