# FIN

`TODO.md`가 현재 남은 작업만 보도록, 길어진 완료 이력과 운영 메모를 이 파일로 옮겼습니다.
더 오래된 상세 이력은 `../docs/todo-done-archive.md`를 참고하면 됩니다.

## Done (2026-04-29)

- [x] `replaceGlobalNote` canonicalization 마이그레이션을 완료함. Workbench는 이제 `character/replace_global_note.risutext`, 로컬 `replaceGlobalNote`, 직접 serialized `data.replaceGlobalNote` 경로를 사용하며, 구형 post-history instruction 경로는 canonical pack/extract/export 동작에서 제거했음. 대상 검증은 변경 TypeScript 파일 `lsp_diagnostics` clean, `rtk npm run --workspace risu-workbench-core test -- tests/charx-blank-character.test.ts tests/charx-extract.test.ts tests/pack-character-roundtrip.test.ts`(3 files/10 tests passed), `rtk npm run --workspace risu-workbench-core build` 통과로 수행함. 전체 core suite 실행에서는 기존 unrelated broad test failure 2건이 남아 있어 `TODO.md` `#### Backlog`에 기록함.
- [x] `replaceGlobalNote` canonicalization 변환계획을 `docs/superpowers/plans/2026-04-29-replace-global-note-canonicalization.md`에 작성하고 사용자 정정사항에 맞게 바로잡음. Workbench canonical은 `replaceGlobalNote` / `character/replace_global_note.risutext` / 직접 serialized `data.replaceGlobalNote`로 복구하며, post-history instruction 경로는 import/export boundary로도 보존하지 않는 Task 1~7 TDD 실행 계획으로 정리함. 구현 소스는 수정하지 않았고, `TODO.md`의 `#### Backlog` 기존 CBS LSP remain tasks는 그대로 남아 있음.
- [x] `.risuvar` default variable hover link 보강을 완료함. `VariableFlowQueryResult`에 `defaultDefinitions`를 추가해 `VariableFlowService.queryVariable()`/`queryAt()` 경로에서 `.risuvar` key 위치를 hover formatter까지 전달하고, hover markdown의 `Default value` 아래에 `command:risuWorkbench.cbs.openOccurrence` 링크가 포함된 `Default definitions` 목록을 렌더링하도록 정리함. Layer 3 agent contract와 test helper shape도 새 배열 필드를 항상-present 배열로 맞췄고, `.risuvar` default-only hover 회귀 테스트는 `variables/defaults.risuvar` 경로와 exact command URI payload를 검증함. 검증은 변경 TypeScript 파일 6개 `lsp_diagnostics` clean, `rtk npm run --workspace cbs-language-server test -- tests/features/hover.test.ts tests/features/definition.test.ts tests/features/completion.test.ts tests/services/variable-flow-service.test.ts`(4 files/189 tests passed), `rtk npm run --workspace cbs-language-server build` 통과로 수행함. `TODO.md`의 `#### Backlog` 기존 CBS LSP 관련 remain tasks는 그대로 남아 있음.
- [x] Character extract canonical-only 출력 보강을 완료함. `phase8_extractCharacterFields`에서 legacy `character/*.txt`, `character/alternate_greetings.json`, `character/metadata.json` 쓰기 블록을 제거하고 `.risuchar`, `character/*.risutext`, `character/alternate_greetings/_order.json` 및 greeting `.risutext`만 남기도록 정리함. Extract/scaffold/CLI dispatch 테스트는 legacy 파일 부재와 canonical marker를 확인하도록 갱신했고, charx 분석 skeleton은 canonical root `.risuchar` 이름을 먼저 읽고 legacy metadata는 fallback으로만 쓰도록 유지함. 검증은 변경 TypeScript 파일 5개 `lsp_diagnostics` clean, `rtk npm run --workspace risu-workbench-core build` 통과, `rtk npm run --workspace risu-workbench-core test -- tests/charx-extract.test.ts tests/cli-main-dispatch.test.ts tests/custom-extension/charx-canonical-pack.test.ts`(3 files/42 tests passed)로 수행함. `TODO.md`의 `#### Backlog` 기존 CBS LSP 관련 remain tasks는 그대로 남아 있음.
- [x] `.risuvar` default-only chat variable discovery 보강을 완료함. `WATCHED_FILE_GLOB_PATTERNS`에 `**/*.risuvar`를 추가해 외부 `.risuvar` create/change/delete도 workspace refresh 입력에 포함되도록 했고, in-memory 실제 `ElementRegistry`/`UnifiedVariableGraph`/`VariableFlowService` fixture helper를 추가해 `.risuvar` key=value 데이터가 completion, go-to-definition, hover tooltip에 연결되는 회귀 테스트를 mock-only가 아닌 실제 서비스 기반으로 고정함. LSP integration watcher registration expectation도 `.risuvar`/`.risutext` 현재 watcher 목록에 맞춰 갱신함. `.risuvar`는 계속 non-CBS-bearing artifact로 유지되며 fake CBS fragment/UnifiedVariableGraph occurrence는 만들지 않음. 검증은 변경 TypeScript 파일 6개 `lsp_diagnostics` clean, `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts tests/features/definition.test.ts tests/features/hover.test.ts tests/services/variable-flow-service.test.ts`(4 files/189 tests passed), `rtk npm run --workspace cbs-language-server build` 통과로 수행함. Required targeted command `rtk npm run --workspace cbs-language-server test -- tests/features/provider-variable-query.test.ts tests/features/completion.test.ts tests/features/definition.test.ts tests/features/hover.test.ts tests/services/variable-flow-service.test.ts tests/lsp-server-integration.test.ts`는 provider/service 5 files가 통과하고 `tests/lsp-server-integration.test.ts` 기존 server seam 15건이 실패해 `TODO.md`의 `#### Backlog`에 별도 remain task로 기록함.
- [x] `.risutext` CBS/variable auto suggest client support를 완료함. VS Code CBS LanguageClient selector에 `.risutext` file pattern과 `risutext` language selector를 추가하고, client-side CBS auto suggest/auto-close language set과 legacy bracket pair highlighter language set에 `risutext`를 포함함. Boundary regression은 trigger suggestions, language configuration, grammar/icon contribution, document selector language/pattern을 보강했고, CBS LSP routing regression은 `.risutext` CBS-bearing recognition, `text` artifact resolution, diagnostics routing, `SUPPORTED_CBS_EXTENSIONS` 포함을 고정함. 검증은 변경 TypeScript 파일 5개 `lsp_diagnostics` clean, `rtk npm run --workspace risu-workbench-vscode test:e2e:cbs-client:boundary`(25 tests passed), `rtk npm run --workspace cbs-language-server test -- tests/custom-extension-routing.test.ts`(23 tests passed), `rtk npm run --workspace risu-workbench-vscode build` 통과로 수행함. `TODO.md`의 `#### Backlog` 기존 CBS LSP full-suite/Lua fragment mismatch 항목은 그대로 남아 있음.
- [x] `.risuchar` / `.risutext` Task 6 conformance prep를 완료함. `docs/schemas/risuchar.schema.json`을 추가해 root metadata owner schema가 identity/version/timestamps/sourceFormat/flags만 요구하고 prose path/field mapping을 요구하지 않게 고정했고, `docs/custom-extension/extensions/text.md`, `docs/custom-extension/README.md`, `docs/custom-extension/targets/charx.md`에 canonical layout, migration, legacy fallback, conflict warning/no-merge, asset/sidecar compatibility, script-safety metadata 경계를 문서화함. Core conformance는 `packages/core/tests/charx-extract.test.ts`와 `packages/core/tests/custom-extension/charx-canonical-pack.test.ts`에 canonical-only, legacy-only, canonical+legacy conflict, alternate greeting order/missing/unlisted append, unknown extensions sidecar, `additionalText`, `triggerscript`, `lowLevelAccess`, asset preservation, `.risuchar` schema expectations을 보강했고, 필요한 최소 pack/extract seam으로 `character/extensions.json` sidecar와 `assets/manifest.json` 기반 CharX asset metadata 복원을 추가함. 검증은 `lsp_diagnostics` 변경 TS 파일 4개 clean, `rtk npm run --workspace risu-workbench-core build` 통과, `rtk npm run --workspace risu-workbench-core test -- tests/charx-extract.test.ts tests/custom-extension/charx-canonical-pack.test.ts`(2 files/19 tests passed), `rtk npm run --workspace cbs-language-server test -- tests/indexer/file-scanner.test.ts tests/custom-extension-diagnostics.test.ts`는 기존 Lua mismatch(`expected 'full', received 'lua-string:1'`) 1건만 실패, focused `.risutext`/`.risuchar` subset(2 files, 6 passed/49 skipped) 통과, `rtk npm run --workspace risu-workbench-vscode build` 통과로 수행함. `TODO.md`의 `#### Backlog` 기존 CBS LSP full-suite/Lua fragment mismatch 항목은 그대로 남아 있음.
- [x] CBS LSP `features/` 폴더 재구성 후속 cleanup으로, imports를 category folders/barrels로 옮긴 뒤 남아 있던 thin compatibility wrapper 28개 root `packages/cbs-lsp/src/features/*.ts` barrel을 Option A에 따라 제거함. Root feature glob은 no files 상태이고, extensionless `features/completion` / `features/hover`는 category folder barrel로 해석되며 `tests/features/rename.test.ts`는 `../../src/features/navigation`을 직접 사용함. 검증은 `lsp_diagnostics packages/cbs-lsp/src/features`(files with errors 0, total diagnostics 3 hints in existing inlayHint file), `lsp_diagnostics packages/cbs-lsp/tests/features`(files with errors 0, total diagnostics 1 hint in existing inlayHint test), `rtk npm run --workspace cbs-language-server test -- tests/features`(18 files passed, 323 tests passed), `rtk npm run --workspace cbs-language-server build`(`> tsc -p .`, exit 0)로 완료함. `TODO.md`는 새 unresolved issue가 없어 변경하지 않았고, 커밋도 만들지 않음.
- [x] CBS LSP `features/` 재구성 최종 검증과 문서 bookkeeping을 완료함. `completion`, `hover`, `navigation`, `symbols`, `editing`, `presentation`, `shared` 7개 폴더가 모두 `index.ts`와 계획된 이동 파일을 포함하고, 기존 `packages/cbs-lsp/src/features/*.ts` root 경로는 모두 `export * from ...` compatibility barrel로 유지됨을 확인함. 최종 검증은 `python3` feature tree scan(`FEATURE_TREE_OK=True`), `python3` root barrel scan(no output), `rtk npm run --workspace cbs-language-server test -- tests/features`(18 files passed, 323 tests passed, failures 0), `rtk npm run --workspace cbs-language-server build`(`> tsc -p .`, exit 0)로 수행함. Task 7의 integration-adjacent 실패는 import migration failure가 아니라 기존 diagnostic taxonomy / LSP server integration backlog로 분류했고, `TODO.md`의 `#### Backlog`에는 CBS LSP full suite fragment-routing/workspace-state/release fixture 조사와 custom-extension diagnostics Lua fragment mapping 재판정 항목이 계속 남아 있음.
- [x] `docs/plans/2026-04-28-risuchar-character-manifest-design.md`를 subagent-driven development implementation plan으로 재구성함. agentic-worker directive, task/subagent boundary, dependency/parallelization map, verification commands, final verification wave, self-review, execution handoff를 포함하도록 정리했으며, 이번 FIN 작업은 문서/계획 이력 기록만 수행했고 구현 소스 파일은 변경하지 않음.

## Done (2026-04-28)

- [x] `docs/plans/2026-04-28-risuchar-character-manifest-design.md`에 `.risuchar` / `.risutext` canonical 설계 구현 시 예상되는 character round-trip 조치사항을 문서화함. Extract -> canonical workspace -> pack/export와 scaffold -> canonical workspace edit -> pack/export 흐름, canonical/legacy 우선순위와 warning, scaffold placeholder의 source-of-truth/pack mapping, `.risuchar` metadata 경계, `.risutext` field ownership, alternate greeting `_order.json` 오류/append 규칙, fidelity 위험, LSP round-trip 결과, 필요한 후속 테스트 범위를 정리했으며 구현 소스와 `TODO.md` 잔여 항목은 변경하지 않음.
- [x] `docs/plans/2026-04-28-risuchar-character-manifest-design.md`를 사용자 논의 결과에 맞춰 `.risuchar` / `.risutext` 최종 설계로 갱신함. `.risuchar`는 기존 `character/metadata.json`을 루트 marker 겸 metadata canonical로 격상하고, 기존 `character/*.txt`와 `alternate_greetings.json`은 frontmatter 없는 CBS-bearing `.risutext` canonical artifact로 승격하는 방향을 확정함. `alternate_greetings/_order.json`은 path 문자열 배열만 소유하며 unlisted `.risutext` greeting은 배열 마지막에 deterministic filename sort로 append하는 정책을 명시했고, legacy split layout은 fallback으로 유지하는 migration/source-of-truth 규칙을 정리함. 구현 소스는 수정하지 않음.
- [x] `docs/superpowers/plans/2026-04-28-cbs-lsp-features-reorganization.md`에 CBS LSP `features/` 폴더 재구성용 subagent-driven implementation plan을 작성함. completion, hover, navigation, symbols, editing, presentation, shared 최종 폴더 구조, root compatibility barrel 정책, task별 subagent boundary, 정확한 대상 파일, focused test/build command, expected output, commit guidance, final TODO/FIN update guidance를 포함했으며 구현 소스는 수정하지 않음. 검증은 plan header read, required folder path count, command presence scan, placeholder phrase scan no matches로 수행함.
- [x] Unified Variable Graph Phase 2 구현과 최종 검증 파를 완료함. provider 공통 variable query helper를 추가하고 definition은 defaults 포함, references는 declaration mode에서만 defaults 포함, rename/hover는 default-only `.risuvar` 제외 정책으로 정렬했으며, definition/references/rename/hover 표면과 hover formatting extraction을 함께 정리함. 검증은 `rtk npm run --workspace cbs-language-server test -- tests/features/provider-variable-query.test.ts`, `rtk npm run --workspace cbs-language-server test -- tests/features/definition.test.ts tests/features/references.test.ts tests/features/rename.test.ts tests/features/hover.test.ts`, `rtk npm run --workspace cbs-language-server build` 통과로 수행함.
- [x] `docs/refactor/unified-var-refactor-list.md`의 PHASE 2 범위만 대상으로 `docs/superpowers/plans/2026-04-28-unified-variable-graph-phase-2.md` subagent-driven implementation plan을 작성함. provider 공통 variable query helper, rename default-only `.risuvar` 제외 정책, references default definition 포함 정책, definition/hover identity 정렬, hover formatting 후처리 분리를 Task 1~5와 final verification wave로 분해했으며 구현 변경은 포함하지 않음.
- [x] `docs/refactor/unified-var-refactor-list.md`의 PHASE 1 범위만 대상으로 `docs/superpowers/plans/2026-04-28-unified-variable-graph-phase-1.md` subagent-driven implementation plan을 작성함. UVG behavior characterization, 죽은 Lua seam 제거, snapshot builder 중복 제거, provider query contract 문서화 작업을 Task 1~4로 분리하고, targeted `unified-variable-graph.test.ts` 검증·cbs-lsp build gate·known full-suite failure boundary를 함께 명시함.
- [x] `docs/superpowers/plans/2026-04-28-diagnostics-router-policy.md`에 Diagnostics Router Policy Extraction Implementation Plan을 작성함. diagnostics-router fragment-to-host remap, workspace issue mapping, diagnostic suppression, suppression trace, subagent-driven implementation tasks, final verification wave 범위를 구현 계획으로 정리했으며 구현 변경은 포함하지 않음.
- [x] CBS LSP completion-context refactor Group F token/fallback detector 정리를 완료함. `block-tag-context.ts`, `plain-text-macro-context.ts`, `fallback-open-brace-context.ts`, `token-macro-argument-context.ts`, `token-keyword-context.ts`를 추가해 BlockEnd/CloseBrace, PlainText incomplete macro recovery, tokenized argument routing, else/block-start/function-name, token-between OpenBrace fallback을 focused detector로 분리했고, `completion-context.ts`는 `CompletionDetectionState` 생성 뒤 calc → slot → call → arg → each → when → token/block/plain-text/else/block-start/function-name → fallback 순서의 detector 배열을 실행하는 thin orchestrator로 축소함. fallback은 마지막 detector로 유지하고 `{ type: 'none' }`은 non-null final result로 처리되게 유지함. 검증은 변경 TypeScript 파일 6개 `lsp_diagnostics`, `completion-context.ts`의 `TokenType.` grep no matches, `rtk npm run --workspace cbs-language-server test -- tests/core/completion-context.test.ts tests/features/completion.test.ts`(141 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] CBS LSP completion-context refactor Group E `when`/`calc`/macro router 추출을 완료함. `packages/cbs-lsp/src/core/completion-context/detectors/calc-expression-context.ts`를 추가해 calc zone 최우선 detector와 `createCalcArgumentContext(...)`를 분리했고, `when-operator-context.ts`로 token/fallback `#when` separator operator routing을 묶었으며, `macro-argument-context.ts`로 variable-first macro argument routing과 metadata/call/calc/#when 특수 fallback을 공용화함. `completion-context.ts`는 calc를 첫 decision으로 유지하고 slot → call → arg → each 순서 뒤 기존 token/fallback 그룹에서 새 helper를 호출하도록 정리함. 검증은 변경 TypeScript 파일 4개 `lsp_diagnostics`, `rtk npm run --workspace cbs-language-server test -- tests/core/completion-context.test.ts tests/features/completion.test.ts`(141 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] CBS LSP completion-context refactor Group D first-argument helper 공통화를 완료함. `packages/cbs-lsp/src/core/completion-context/detectors/first-argument-context.ts`를 추가해 `slot::`, `call::`, `arg::` 첫 번째 argument 판별 흐름(open brace, macro name, immediate separator, separator 이후 cursor, 두 번째 separator reject, typed argument prefix/empty prefix)을 공유 helper로 묶었고, 각 detector는 enable 조건·result type wiring만 유지함. `arg::` active local function guard와 guard 실패 시 `{ type: 'none' }` 반환은 `argument-reference-context.ts`에 그대로 남김. 검증은 변경 TypeScript 파일 4개 `lsp_diagnostics`, `rtk npm run --workspace cbs-language-server test -- tests/core/completion-context.test.ts tests/features/completion.test.ts`(141 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] CBS LSP completion-context refactor Group C 독립 detector 추출을 완료함. `each iterator`, `slot alias`, `call function argument`, `argument reference` detector를 `packages/cbs-lsp/src/core/completion-context/detectors/` 하위 4개 파일로 분리하고, `completion-context.ts`에서는 calc 이후 slot → call → arg → each 순서로 `CompletionDetectionState`를 전달해 호출하도록 정리함. `arg::` active local function guard와 `{ type: 'none' }` 동작, first-argument 중복 로직, public `CompletionTriggerContext` export path는 유지함. 검증은 `lsp_diagnostics`(변경 TypeScript 파일 5개), `rtk npm run --workspace cbs-language-server test -- tests/core/completion-context.test.ts tests/features/completion.test.ts`(141 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] CBS LSP completion-context refactor Group B `CompletionDetectionState` seam을 완료함. `packages/cbs-lsp/src/core/completion-context/detection-state.ts`를 추가해 cursor/token/node-path 준비 상태와 `offsetOf`, open brace/separator lookup, parent macro/block/when lookup, typed prefix, argument index inference, variable argument context 생성 helper를 추출했고, `completion-context.ts`의 detector bodies와 public export path는 유지한 채 state method 호출로만 연결함. 검증은 `lsp_diagnostics`(`completion-context.ts`, `completion-context/detection-state.ts`), `rtk npm run --workspace cbs-language-server test -- tests/core/completion-context.test.ts tests/features/completion.test.ts`(141 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] CBS LSP completion-context refactor Group A characterization gate를 완료함. `packages/cbs-lsp/tests/core/completion-context.test.ts`를 추가해 `detectCompletionTriggerContext` 직접 결과를 helper 추출 전 기준으로 고정했고, bare/partial root, block prefix, close/else, metadata/call/arg/slot first-argument, calc, `#when`, token-between fallback, `CloseBrace` argument priority, `arg::` outside active local function의 `{ type: 'none' }` suppression을 포함함. 검증은 `lsp_diagnostics`(completion-context.test.ts), `rtk npm run --workspace cbs-language-server test -- tests/core/completion-context.test.ts tests/features/completion.test.ts`(139 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `docs/refactor/server-helper-refactor.md` 계획에 따라 `ServerFeatureRegistrar`를 thin composition root로 축소하고, 17개 feature registrar를 `packages/cbs-lsp/src/helpers/server/registrars/`로 분리함. `WorkspaceAwareProviderFactory`는 provider 생성과 CBS completion payload enrich를 담당하고, `LuaLsRequestGate`는 LuaLS proxy routing/skip을 중앙 관리하며, `LuaLsResponseMerge`는 hover/definition/completion merge helper를 순수 함수로 유지하고, `LuaLsFallbackService`는 LuaLS 관련 handler의 fallback/merge orchestration을 담당함. 최종 검증 파 F1~F4가 승인됐고, `tests/helpers/server-helper.test.ts` 등 targeted test 36개 통과, `src/helpers/server/` 22개 파일 LSP diagnostics clean, `rtk npm run --workspace cbs-language-server build` 검증을 완료함.
- [x] CBS LSP completion refactor T8 final facade cleanup을 마무리함. `packages/cbs-lsp/src/features/completion-candidates.ts`를 추가해 후보 생성 흐름을 분리하고, `completion.ts`는 public API와 thin facade/orchestrator 책임만 남기도록 정리함. 검증은 F3 보강 뒤 `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(124 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] CBS LSP completion refactor final verification wave를 완료함. F1 facade responsibility, F2 dependency cycle, F3 regression test, F4 public contract review가 최종 승인됐고, F3 재검토 전 누락됐던 variable resolve hydration 회귀 테스트 2건을 보강해 targeted completion test가 124개 통과 상태로 정렬됨.
- [x] `packages/cbs-lsp/src/features/completion-resolve.ts`를 신설해 completion lazy resolve/unresolved payload 축소를 provider 밖으로 분리함. unresolved item은 detail/documentation/explanation/workspace snapshot을 계속 제거하면서 uri/position/category만 보존하고, resolve는 uri/position 검증 뒤 builtin/local/workspace variable hydrate를 기존 observable LSP field 계약 그대로 수행하도록 `completion.ts`의 public `provideUnresolved()`/`resolve()` wrapper에서 새 helper로 위임함. 기존 lazy-resolve 회귀 테스트가 heavy field stripping, lazy builtin docs, textEdit 보존, mismatch rejection을 이미 고정해 별도 테스트 추가는 하지 않음. 검증은 `lsp_diagnostics`(`completion.ts`, `completion-resolve.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/src/features/cheap-macro-argument-completion.ts`를 신설해 oversized `.risulua` macro argument fast path의 context detection, scan cap, known macro guards, nested chat value 후보, workspace variable 후보 생성, current-line textEdit 적용을 provider 밖으로 분리함. `completion.ts`는 metadata key / `#when` operator builder와 toggle segment 판별, workspace variable callbacks만 새 helper에 주입하도록 정리했고, normal fragment-analysis argument completion과 resolve logic은 건드리지 않음. 검증은 `lsp_diagnostics`(`completion.ts`, `cheap-macro-argument-completion.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/src/features/cheap-root-completion.ts`를 신설해 cheap root context detection, sectioned artifact CBS section guard, pure/puredisplay block body suppression, line/offset helpers, root fast path textEdit 적용을 provider 밖으로 분리함. `completion.ts`는 `{{` / `{{#` fast path에서 registry와 builtin metadata callbacks만 새 helper에 주입하고, oversized `.risulua` cheap macro argument 로직은 범위 밖으로 유지하면서 공용 line helper만 재사용함. 검증은 `lsp_diagnostics`(`completion.ts`, `cheap-root-completion.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/src/features/workspace-variable-completion.ts`를 신설해 workspace chat variable, risutoggle global variable, risutoggle raw name completion 후보 생성과 workspace variable documentation/source/explanation, stale availability, `zzzz-workspace-` sort prefix contract를 provider 밖으로 분리함. `completion.ts`는 fragment-local symbol-table 후보를 유지하면서 local label dedupe set과 workspace summaries/callbacks만 새 builder에 주입하도록 정리했고, stale calc-expression workspace 후보 suppression과 non-calc stale metadata 노출 정책을 유지함. 검증은 `lsp_diagnostics`(`completion.ts`, `workspace-variable-completion.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] T3 builtin completion extraction verification retry에서 `completion.ts`의 unused private wrapper 진단을 정리함. 기존 lazy unresolved-completion 테스트의 spy seam은 유지하되 wrapper를 public delegate로 두고 실제 documentation formatting은 `builtin-completion.ts`의 module formatter에 계속 위임하도록 조정함. 검증은 `lsp_diagnostics`(`completion.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/src/features/builtin-completion.ts`를 신설해 registry builtin, static block snippet, `:else`, close-tag completion item 생성과 builtin detail/documentation/category/explanation/deprecation metadata 처리를 provider 밖으로 분리함. `completion.ts`는 새 builder 함수에 registry와 metadata 생성 콜백만 주입하도록 정리했고, workspace variable/cheap macro argument/resolve hydration 외 동작은 유지함. 검증은 `lsp_diagnostics`(`completion.ts`, `builtin-completion.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/src/features/completion-text-edit.ts`를 신설해 completion textEdit 순수 로직(`CompletionTextEditPlan`, ranged context type, full macro/block snippet replacement helper, normal range newText normalization, full macro snippet 판별)을 분리함. `completion.ts`는 추출된 helper를 import해 기존 full macro auto-close 교체, block header 교체, 일반 context range 교체 계약을 유지했고, deprecated flag 접근 helper를 통해 변경 파일 LSP hint도 정리함. 검증은 `lsp_diagnostics`(`completion.ts`, `completion-text-edit.ts`), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/tests/features/completion.test.ts`에 completion refactor 전 baseline용 normalized snapshot coverage를 보강함. `snapshotCompletionItems`로 block builtin/snippet, getvar local+workspace chat variable, calc `$` local+workspace variable, `#when` operator/variable, risutoggle raw name, close tag, oversized `.risulua` cheap macro argument, unresolved/resolved `getvar` contract를 고정했고, sectioned `.risulorebook`의 non-CBS `@@@ KEYS` 섹션에서 cheap root fast path가 fragment analysis를 우회하지 않는 negative test를 추가함. 검증은 `lsp_diagnostics`(completion.test.ts), `rtk npm run --workspace cbs-language-server test -- tests/features/completion.test.ts`(122 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.

## Archived from `TODO.md` on 2026-04-18

### Condensed completed history moved out of `TODO.md`

#### Recent CBS LSP work

- [x] CBS LSP cross-artifact server/provider feature matrix coverage
- [x] CBS LSP fragment-local server capability wiring + cached analysis lifecycle
- [x] CBS LSP fragment-local scope analysis + symbol-backed variable diagnostics
- [x] CBS LSP fragment-aware semantic tokens/folding 구현
- [x] CBS LSP registry/shared-lookup hover provider 구현
- [x] CBS LSP registry-driven signature help 구현
- [x] CBS LSP adapter-backed diagnostics analyzer 확장
- [x] CBS LSP curated fixture corpus + Wave 1 red-test matrix freeze
- [x] CBS LSP cursor→fragment/token/node locator seam 추가
- [x] CBS LSP host↔fragment remap utility centralization
- [x] CBS LSP fragment analysis service contract 추가
- [x] CBS LSP diagnostic taxonomy/deferred-scope contract freeze
- [x] CBS LSP Phase 2~3 병렬계획 handoff 작성
- [x] CBS_LSP_PLAN을 custom-extension fragment baseline 기준으로 재정렬
- [x] CBS LSP 치트시트를 custom-extension canonical 기준으로 정렬
- [x] CBS LSP 치트시트에 LuaLS 추후 방향 정리 추가

#### Recent analyzer / wiki / relationship-network work

- [x] Relationship-network node gather context menu
- [x] Regex ownership seam cleanup
- [x] 정적 도구 한정 발상 보조 아이디어 20선 추가 작성
- [x] RisuAI Workbench 발상 증폭용 생산성 보조 도구 제안서 작성
- [x] RisuAI Workbench 초강력 개발지원도구 제안서 작성
- [x] Lorebook activation Walk direct-reference cleanup
- [x] Analyze Lua lorebook edge expansion
- [x] Folder-aware lorebook wiki migration
- [x] Chain index + folder-aware lorebook-activation follow-up
- [x] 260406 Wiki Runner Persistent Output Fix
- [x] 260406 Wiki Test-Plan Integration
- [x] Final Wave blocker/wiki/analyze follow-up fixes
- [x] Relationship Network Root Group Pin Fix
- [x] Relationship Network Hidden Trigger Physics Fix
- [x] Wiki lorebook link slug fix

#### Product / architecture / packaging milestones still cluttering `TODO.md`

- [x] Vite 8 webview build 도입
- [x] Lorebook Path-Based Workspace Contract Migration (T16)
- [x] F2 Blocker Fix: Lua/CBS documentation truthfulness alignment
- [x] F4 Blocker Fix: T16 guard test deferred-scope alignment
- [x] 루트 `AGENTS.md` 추가
- [x] extract / pack / analyze-card / preset / module 관련 초기 기반 작업 묶음
- [x] `packages/core` 구조 문서 / 테스트 전략 / 파이프라인 문서 정리
- [x] Monorepo Restructure (Tasks 1-15)
- [x] `packages/core/src/shared/phase-helpers.ts` 분해 이관 완료
- [x] `packages/core` Task 9 완료: `cli/analyze` 순수 로직 domain 추출

### Completed items removed from `### Remaining`

#### Repack Contract & Validation

- [x] 병합 우선순위(contract) 문서화 (`card.json` vs 추출 컴포넌트)
- [x] `--out` 경로 해석 규칙 문서화 (파일 경로 vs 디렉토리)
- [x] `pack -> extract` 검증 체크리스트 문서화

#### Architecture Restructure (source of truth: `docs/architecture-proposal.md`)

- [x] `template/` 퇴역 결정 반영: 루트 역할을 scaffold 패키지에서 workspace/product 루트로 재정의
- [x] `bin/create.js`, `template/`, README의 scaffold 흐름 제거/축소 계획 수립
- [x] 구조 제안 문서 작성 (`docs/architecture-proposal.md`)
- [x] 프로젝트 철학/제품 방향성 Octto 세션 및 설계 문서 작성 (`../docs/plans/2026-03-17-risuai-workbench-philosophy-design.md`)
- [x] 프로젝트 정체성 정교화 Octto 세션 및 설계 문서 작성 (`docs/plans/2026-03-18-risuai-workbench-identity-design.md`)
- [x] Phase 1: Core 내부 정리 (domain/node/cli 분리, scripts/ 제거)
- [x] Phase 2 일부 완료: `services/`, `providers/tree-provider.ts`, `commands/`, `panels/card-panel.ts`

### Reference notes moved from `TODO.md`

- 2025-04-14 build note: `tsc-alias` resolution fix (`packages/core/package.json`, `packages/vscode/package.json` build scripts switched to `npx tsc-alias`)

## Archived from `TODO.md` on 2026-04-24

### Done (2026-04-27)

- [x] `docs/refactor/element-registry-refactor.md`에 ElementRegistry 책임 분리 오케스트레이션을 다루는 subagent-driven development 상위 계획을 추가함. characterization/type seam, 병렬 builder slice, 순차 gate, verification wave를 문서 전용 계획 범위로 정리했으며 구현 변경은 포함하지 않음.
- [x] `docs/refactor/completion-context-refactor.md` 최상단에 subagent-driven development용 작업계획을 추가함. 병렬 가능한 characterization tests, `DetectionState`/helper 추출, 독립 detector, first-argument helper, when/calc/macro router, token/fallback detector slice와 순차 통합 gate, handoff 규칙, 검증 기대값을 구현 없이 정리함.
- [x] `WorkspaceRefreshController`를 workspace refresh event-routing façade로 축소하고, refresh contract / scheduler / affected URI 계산 / watched-file request adapter / refresh effects / `WorkspaceRefreshExecutor`를 `packages/cbs-lsp/src/controllers/workspace-refresh/` 모듈 세트로 분리함. open local-first diagnostics, change debounce, close/watched-file/tracked flush 순서, root별 LuaLS `prioritySourceUris`, LuaLS sync trace payload, diagnostics publish, CodeLens scheduling 동작을 유지했고 scheduler·affectedUris·watched-file adapter·executor·controller focused tests를 추가함. 검증은 `lsp_diagnostics`(변경 src/tests 파일), `rtk vitest run tests/controllers/workspace-refresh-controller.test.ts tests/controllers/workspace-refresh-scheduler.test.ts tests/controllers/workspace-refresh-affected-uris.test.ts tests/controllers/workspace-refresh-watched-files.test.ts tests/controllers/workspace-refresh-executor.test.ts`(14 pass), `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `docs/refactor/fragment-analysis.md`에 문서/계획 작업 전용 subagent-driven development 상위 계획을 추가함. 병렬 investigation, 순차 의존성, subagent deliverable, coordination rule, verification wave, completion checklist 범위를 구현 없이 정리함.
- [x] `docs/refactor/fragment-analysis.md`를 search/analyze findings 기반의 더 풍부한 subagent-driven plan으로 확장함. 실제 fragment-analysis source/test surface, 알려진 backlog gap, 병렬 group, 순차 gate, handoff rule, verification wave를 문서/계획 작업으로만 정리했고 구현 변경은 포함하지 않음.
- [x] `docs/refactor/fin/fragment-analysis.md` final orchestration packet을 완료 기록에 맞게 정리함. 분석/문서 전용 경계를 유지했고 구현 변경은 포함하지 않음.
- [x] `docs/refactor/server-helper-refactor.md` 최상단에 subagent-driven development용 작업계획을 추가함. 병렬 가능한 characterization, 단순 registrar 추출, 중간 복잡도 registrar 추출, provider factory/type, LuaLS helper 준비 slice를 분리하고, complex LuaLS fallback/merge/rename/completion 작업은 characterization test와 단순 registrar 추출 뒤에 통합하도록 순차 gate와 handoff 규칙을 명시함.
- [x] `docs/refactor/workspaceRefesher.md` 최상단에 subagent-driven development용 작업 계획을 추가함. 병렬 가능한 characterization/affectedUris/RefreshScheduler/watched-file adapter slice와 순차 통합 gate(refresh contract, executor 이동, effect 분리, controller façade 축소, 검증)를 분리하고, open/change/close/watched-file/tracked refresh 순서 보존 규칙과 subagent handoff 규칙을 명시함.
- [x] `.risulua` LuaLS sidecar cross-file 결과 remap을 보강함. `LuaLsDocumentRouter`가 현재 standalone/workspace mirror 상태에서 `transportUri -> sourceUri` reverse map을 제공하고, `LuaLsProxy`가 요청 단위 remap context로 definition/references/rename WorkspaceEdit/documentSymbol(`SymbolInformation[]`)/hover/completion 표시 문자열을 source `.risulua` URI 기준으로 정규화하도록 정리함. 응답 remap helper는 `lualsResponseRemapper.ts`로 분리했고, `CompletionItem.data` 같은 opaque payload는 보존함. 검증은 LSP diagnostics, `rtk npm run --workspace cbs-language-server test -- tests/providers/luals-proxy.test.ts tests/providers/luals-documents.test.ts tests/helpers/server-helper.test.ts tests/controllers/luals-companion-controller.test.ts`, `rtk npm run --workspace cbs-language-server build`로 수행함.
- [x] `docs/refactor-targets.md`를 추가해 `packages/cbs-lsp` 리팩토링 후보를 문서화함. `TODO.md`의 `#### Backlog` 실패 항목과 직접 연결되는 correctness hotspot(`lualsProxy.ts`, `fragment-analysis-service.ts`, `WorkspaceRefreshController.ts`, `diagnostics-router.ts`, local-first provider 계층)을 우선순위로 두고, `completion.ts`, `server-helper.ts`, `completion-context.ts` 같은 대형 구조 부채는 후속 후보로 분리해 권장 진행 순서를 정리함.
- [x] `packages/cbs-lsp` 내부 리팩토링 우선순위 조사를 수행함. `TODO.md`의 cbs-language-server 잔여 실패 항목을 기준으로 구조 부채(`completion.ts`, `server-helper.ts`, `completion-context.ts`)와 현재 실패 연결성이 높은 소스(`lualsProxy.ts`, `fragment-analysis-service.ts`, `WorkspaceRefreshController.ts`, `diagnostics-router.ts`, `local-first-contract.ts` 계열)를 구분해 후보군과 우선순위 근거를 정리함.
- [x] `packages/cbs-lsp/src/contracts/`를 신설해 agent-facing protocol metadata와 Layer 1/3 contract descriptor를 장기 공용 계약 계층으로 이동함. `agent-contracts.ts`가 더 이상 `core`에 의존하지 않도록 `agent-metadata.ts`를 같은 contracts 계층에서 참조하게 했고, `core/index.ts`, CLI report/query, fixture helper, diagnostics metadata, `unified-variable-graph.ts`, README/checklist 경로를 새 구조에 맞춰 정리함. 검증은 `lsp_diagnostics`, `npm run --workspace cbs-language-server build`, 관련 contract 테스트로 수행했으며, fixture corpus 전체 테스트의 기존 Lua fragment 기대값 불일치는 루트 `TODO.md`의 Backlog 항목으로 남아 있음을 재확인함.
- [x] `packages/cbs-lsp/src/analyzer/scope/visible-loop-bindings.ts`, `reference-collector.ts`, `definition-collector.ts`, `analyzable-body-resolver.ts`, `scope-macro-rules.ts`, `scope-frame.ts`, `static-argument.ts`, `lookup-key.ts`의 AGENTS.md JSDoc 규약 준수 상태를 보강함. exported type/interface/class/function 주석과 scope 분석 분기 의도 주석을 추가했고, LSP 진단과 build로 검증함.
- [x] `packages/cbs-lsp/src/core/availability-contract.ts`, `packages/cbs-lsp/src/core/pure-mode.ts`, `packages/cbs-lsp/src/core/fragment-locator.ts`의 AGENTS.md JSDoc 규약 준수 상태를 보강함. 파일 개요 주석, exported type/interface/function 주석, fragment locator 내부 복잡 로직 helper 설명을 추가했고, `lsp_diagnostics`와 `rtk npm run --workspace cbs-language-server build`로 검증함.
- [x] `packages/cbs-lsp/src/core/host-fragment-patch.ts`, `packages/cbs-lsp/src/core/cbs-formatter.ts`, `packages/cbs-lsp/src/core/index.ts`의 AGENTS.md JSDoc 규약 준수 상태를 점검하고, 부족했던 exported interface/type 및 barrel module 개요 주석을 보강함. `lsp_diagnostics`와 `rtk npm run --workspace cbs-language-server build`로 검증함.
- [x] `packages/cbs-lsp/src/analyzer/diagnostics/collectors/`의 block, macro, legacy-angle, parser-diagnostic collector와 `packages/cbs-lsp/src/analyzer/block-header/`의 block header, barrel, function declaration 파일 JSDoc 규약 준수 상태를 보강함. exported constants/interface 설명과 diagnostics helper 함수 설명을 추가했고, `lsp_diagnostics`와 `rtk npm run --workspace cbs-language-server build`로 검증함.
- [x] `packages/cbs-lsp/src/analyzer/diagnostics/`의 `builtin-helpers.ts`, `calc-expression-diagnostics.ts`, `symbol-diagnostics.ts`, `diagnostics-engine.ts`, `context.ts`, `block-header.ts`, `pure-mode-filter.ts`, `taxonomy.ts` JSDoc 상태를 점검하고, 누락된 exported interface/type/enum/const/class 및 복잡 로직 설명 주석을 보강함. 검증은 `lsp_diagnostics`와 `npm run --workspace cbs-language-server build`로 수행함.
- [x] `packages/cbs-lsp/src/analyzer/symbolTable.ts`, `scopeAnalyzer.ts`, `diagnostics.ts`, `diagnostics/index.ts`, `diagnostics/quick-fix.ts`, `diagnostics/diagnostic-info.ts`, `diagnostics/compare.ts`의 AGENTS.md JSDoc 규약 준수 상태를 보강함. 파일 개요 주석을 유지하고 누락된 exported type/interface 및 barrel export 설명 주석을 추가함. 검증은 `lsp_diagnostics`와 `npm run --workspace cbs-language-server build`로 수행함.

### Done (2026-04-18)

- [x] cbs-lsp feature별 trace/log 구분을 server layer에 추가하고 integration test로 emission을 고정
- [x] cbs-lsp multi-fragment 문서에서 unchanged fragment의 parse/analyze 결과를 재사용하는 incremental path와 회귀 테스트를 추가
- [x] cbs-lsp request cancellation 경로를 server/provider/service에 연결하고 canceled request no-op 회귀 테스트를 추가
- [x] cbs-lsp diagnostics/feature cache invalidation 시점을 text signature 검증과 shutdown 정리까지 포함해 명확히 하고 회귀 테스트를 추가
- [x] malformed custom-extension section에서도 뒤따르는 유효 CBS fragment recovery를 유지하도록 core fragment mapper를 보강하고 cbs-lsp 회귀 테스트를 추가
- [x] module canonical Lua extract/analyze 경로를 `trigger[]` 기반 upstream shape에 맞게 정렬하고 `getState`/`setState` wrapper 분석 인덱싱을 수정
- [x] analyze 리포트에 lorebook↔lua / lua↔regex risuvar 브릿지를 노출하고 lorebook `setdefaultvar` 추적을 추가
- [x] relationship-network에 lorebook↔lua / lua↔regex bridge edge와 report-shell legend 스타일을 추가
- [x] relationship-network bridge edge가 실제 analyze 산출물에서 누락되던 Lua baseName→function node 매핑 버그를 수정하고 회귀 테스트를 추가
- [x] triggerId-first `setChatVar/getChatVar` 인자 해석을 수정해 실제 playground module analyze에서 Lua state/bridge edge가 생성되도록 복구
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`를 current/deferred/future 상태 추적용 문서로 재정렬하고 상태 태그를 추가
- [x] `packages/core/src/domain/analyze/correlation.ts`의 pair correlation direction 정렬 타입 오류를 수정하고 `packages/core` 빌드를 복구

### Done (2026-04-19)

- [x] `docs/upstream-traceability/registries/upstream-anchors.md`, `local-references.md`, `field-entrypoints.md`, `review-ledger.md`와 `ledgers/cross-cutting-gaps.md`를 추가해 허브 공통 장부를 작성하고 CBS local reference 공백, preset process anchor uncertainty, module variable bridge uncertainty를 중앙 ledger로 정리
- [x] `docs/upstream-traceability/index.md`를 작성하고 target/artifact/domain 문서의 ledger 부재 메모와 registry bootstrap 상태를 실제 허브 구조에 맞게 정렬
- [x] cbs-lsp Layer 1 `ElementRegistry`를 추가해 FileScanner scan result를 URI/artifact/graph-seed 중심 read model로 정규화하고 lorebook/regex/prompt/html fragment CBS read-write, lua 파일 분석, non-CBS empty-element contract를 회귀 테스트·체크리스트 Evidence·README와 함께 고정
- [x] cbs-lsp runtime availability contract를 공용 모듈로 정리해 initialize `experimental.cbs.availability`, server trace payload, normalized host diagnostics snapshot이 같은 `local-only`/`deferred`/`workspace-disabled` 상태를 재사용하도록 맞추고 회귀 테스트·체크리스트 Evidence를 함께 갱신
- [x] cbs-lsp availability honesty contract를 추가해 definition/references/rename provider의 local-only 범위와 formatting/deferred capability, risutoggle/risuvar workspace-disabled 상태를 README·server experimental capability·회귀 테스트에서 같은 용어로 정렬
- [x] cbs-lsp Layer 1 `FileScanner`를 추가해 workspace canonical `.risu*` 파일의 artifact 분류와 `hasCbsFragments`/`fragmentMap` scan contract를 고정하고 회귀 테스트·체크리스트 Evidence를 함께 갱신
- [x] cbs-lsp diagnostic quick-fix payload에 공통 explanation metadata를 추가해 deprecated replacement / alias canonicalization / registry suggestion / legacy syntax migration provenance를 machine-readable하게 노출하고 host snapshot·회귀 테스트를 함께 갱신
- [x] cbs-lsp completion/hover/diagnostics payload에 공통 explanation metadata를 추가해 registry lookup / scope analysis / contextual inference / diagnostic taxonomy provenance를 machine-readable하게 노출하고 snapshot helper·회귀 테스트를 함께 갱신
- [x] cbs-lsp completion/hover/host diagnostics에 deterministic normalized snapshot view helper를 추가하고 fixture corpus 기반 회귀 테스트로 category contract·stable ordering·stable field names를 함께 고정
- [x] cbs-lsp completion/hover payload에 공통 agent-friendly category contract를 추가해 builtin / variable / block keyword / contextual token 분류를 `data`에서 바로 읽을 수 있게 하고 관련 회귀 테스트·checklist Evidence를 함께 갱신
- [x] cbs-lsp signature help가 `#each`/`#func` block header와 `call::name` 로컬 호출 문맥을 구분해서 iterator/alias, functionName/arg slot 의미를 직접 설명하도록 보강하고 completion wording·회귀 테스트·체크리스트 Evidence를 함께 갱신
- [x] cbs-lsp diagnostics가 registry `docOnly` source-of-truth를 직접 읽어 문서용 syntax entry와 호출 가능한 builtin wording을 분기하고, hover 대표 항목 회귀 테스트를 보강
- [x] cbs-lsp completion이 registry `docOnly` source-of-truth를 읽어 문서용 syntax entry와 호출 가능한 builtin wording을 detail/documentation에서 구분하도록 정리하고 회귀 테스트를 추가
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md` 서론에 작업 완료 후 다음 체크리스트 항목을 작업요청 템플릿 형태로 제안하도록 안내 문구와 예시 템플릿을 추가
- [x] cbs-lsp fragment analysis에 공통 recovery contract(`clean`/`token-recovery`/`structure-recovery`)를 추가하고 completion/hover/folding이 같은 malformed 문맥 신뢰도(`tokenContextReliable`/`structureReliable`)를 읽도록 정리한 뒤 관련 회귀 테스트를 추가
- [x] cbs-lsp diagnostics payload에 taxonomy 기반 normalized rule metadata(`category`/`code`/`owner`/`severity`/`meaning`)를 추가하고 fixture corpus·taxonomy 테스트가 engine/LSP payload에서 같은 계약을 검증하도록 정리
- [x] cbs-lsp diagnostic payload에 quick-fix용 machine-readable metadata(`replacement`/`suggestions`/`editKind`)를 추가하고 deprecated/alias/legacy/unknown builtin 진단이 engine→LSP payload까지 같은 fix 정보를 유지하도록 회귀 테스트를 추가
- [x] cbs-lsp diagnostic payload에 optional relatedInformation 구조를 추가하고 unused variable secondary definition을 engine/LSP payload까지 remap하는 회귀 테스트를 추가
- [x] cbs-lsp calc expression의 잘못된 `$`/`@` reference token을 incomplete/invalid identifier 진단으로 세분화하고 inline/macro parity 회귀 테스트를 추가
- [x] cbs-lsp diagnostics/hover wording과 empty calc handling을 정리해 `{{? ...}}` / `{{calc::...}}`가 같은 CBS expression sublanguage taxonomy를 쓰도록 맞추고 회귀 테스트를 추가
- [x] cbs-lsp calc zone diagnostics를 전용 taxonomy(CBS008~CBS011)로 세분화하고 `{{? ...}}` / `{{calc::...}}`에 더 구체적인 message/range 회귀 테스트를 추가
- [x] cbs-lsp pure-mode 공용 규칙을 `src/core/pure-mode.ts`로 올리고 semantic tokens / diagnostics / hover / completion / folding이 같은 억제·예외(`slot::`, `call::`, `arg::`) 계약을 보도록 정렬
- [x] cbs-lsp calc expression 공용 해석기를 추가하고 `{{? ...}}` / `{{calc::...}}`를 diagnostics, completion, hover에서 같은 규칙으로 처리하도록 정렬
- [x] cbs-lsp local `#func` 심볼과 `call::name` 참조를 연결해서 scope analysis / completion / hover / deferred definition이 같은 local-function lookup을 보도록 구현하고 회귀 테스트를 추가
- [x] cbs-lsp `arg::N`을 0-based local-call argument slot으로 정렬해서 diagnostics / completion / signature help가 같은 계약을 쓰도록 구현하고 회귀 테스트를 추가
- [x] cbs-lsp `call::name` / `arg::N` hover·semantic token wording을 로컬 함수 참조 / N번째 인자 참조 의미가 드러나게 정리
- [x] cbs-lsp `#func name` 선언 hover와 `arg::N` 참조 hover가 서로 다른 목적 문구와 로컬 파라미터 정의 위치를 함께 보여주도록 보강하고 관련 hover/signature 회귀 테스트를 추가
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 `call::name` / `arg::N` 관련 애매한 체크 항목을 기능별 세부 항목으로 분해하고 문구를 더 직관적으로 정리
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 diagnostics 관련 축약 항목을 expression 진단 / payload / recovery / `doc_only` 선행 조건 기준으로 더 구체적으로 분해
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 completion 관련 축약 항목에 구현 touchpoint를 붙여 LLM이 어느 레이어를 보완해야 하는지 바로 보이게 정리
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 `doc_only` completion 항목을 "문서용 특수 항목 vs 실제 호출 가능한 builtin" 구분과 선행 메타 설계가 드러나게 정리
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 2.3 hover/signature 항목을 token 의미와 구현 touchpoint가 드러나는 작업 단위로 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 2.4 semantic tokens / folding 항목을 token 매핑 문서화와 cross-feature 충돌 점검 기준이 드러나게 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 2.2 completion 섹션을 builtin/block/variable-expression/metadata 정책 기준의 작업 단위로 재구성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 Deferred 3.1/3.2 섹션을 "provider는 있으나 미노출" / "host patch semantics가 blocker"라는 구조가 드러나게 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 4.1 document symbol / outline 섹션을 신규 provider 작업과 fragment-aware outline 요구가 드러나게 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 4.2 code actions / quick fixes 섹션을 자동 치환형 quick fix / 설명형 action / edit model blocker 구조로 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 4.3 agent-friendly metadata 섹션을 LLM이 읽을 output contract 관점으로 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.1/5.2 architecture 섹션을 workspace indexer 수집 계층과 cross-file 서비스 계층의 책임이 드러나게 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.3 Lua bridge 섹션을 LuaLS companion + CBS overlay 책임 분리가 드러나게 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`에 LuaLS 통합의 최소 검증 루프를 다루는 5.4 MVP 섹션을 추가하고 기존 5.4를 5.5로 조정
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`에서 중복되던 7.x 추천 섹션을 제거하고 5.5/5.6이 문서의 자연스러운 마무리 단계가 되도록 구조를 정리
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 1.3 `call::name` / `arg::N` / pure-mode 항목에 current gap과 구현 touchpoint를 붙여 실행 가능하게 정리
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 2.1 diagnostics 세부 항목을 calc 진단 / payload shape / recovery consistency / `doc_only` blocker 관점으로 구체화
- [x] malformed recovery 이후에도 cbs-lsp diagnostics가 deterministic order / related range / quick-fix suggestion payload를 유지하도록 정렬·정규화 정책을 추가하고 stability 회귀 테스트 및 deferred `call::name` definition 해석 누락을 함께 수정
- [x] cbs-lsp `slot::` completion을 loop alias 전용 정책으로 분리하고 현재/nested/malformed `#each` scope ordering을 회귀 테스트로 고정
- [x] core CBS builtin registry에 `docOnly` 메타와 helper를 추가해 `#when`/`#each`/`slot`/pure 계열을 source-of-truth로 분류하고 hover 문서화·체크리스트 blocker를 함께 갱신
- [x] cbs-lsp `slot::name` hover가 builtin `slot` 문서와 분리된 loop alias reference 설명을 보여주고, nested `#each` shadowing definition/hover 회귀 테스트와 체크리스트 Evidence를 함께 갱신
- [x] cbs-lsp `{{? ...}}` / `{{calc::...}}` hover·signature가 공용 calc expression sublanguage 문구를 공유하도록 정리하고 회귀 테스트·체크리스트 Evidence를 갱신
- [x] cbs-lsp `arg::` completion이 active local `#func` / `{{call::...}}` 문맥 기준 0-based slot 후보만 제안하도록 정리하고 wording parity·non-local suppression 회귀 테스트와 체크리스트 Evidence를 갱신
- [x] cbs-lsp semantic token 매핑 표와 특수 분류 규칙(variable builtin 첫 인수 / block header operator / pure-mode body)을 `semanticTokens.ts`에 고정하고 회귀 테스트·체크리스트 Evidence를 갱신
- [x] cbs-lsp folding / hover / diagnostics pure-mode 계약을 공용 helper로 정렬하고 `#when` / `#each` / `#func` / malformed / multi-fragment 회귀 테스트와 checklist Evidence를 갱신
- [x] `docs/superpowers/specs/2026-04-19-upstream-traceability-design.md`에 workspace-wide upstream 추적성 문서 체계 spec을 작성하고 인덱스+심화 문서 구조, trace table 근거 모델, gap/unverified 운영, 유지보수 workflow를 정리
- [x] `docs/superpowers/specs/2026-04-19-upstream-traceability-design.md`에 local reference anchor(`docs/cbs_pipeline.md`), source tier/contract status, reverse lookup, patch watchpoint, cross-cutting gap ledger 개념을 추가해 upstream patch-following과 참조 경로 탐색 목적을 명확히 정리
- [x] `docs/superpowers/specs/2026-04-19-upstream-traceability-design.md`를 `docs/upstream-traceability/index.md` 중심의 wiki-lite traceability hub 구조로 재정렬하고 `registries/targets/artifacts/domains/ledgers` 계층 및 `preset` 1급 target 구조를 명시
- [x] `docs/superpowers/specs/2026-04-19-upstream-traceability-design.md` 최상단에 독립 leaf 문서는 `Sisyphus-Junior (category: quick)` subagent 병렬 생성 가능, 단 `index`/`registries`/`ledgers`/최종 정합성 통합은 순차 정리해야 한다는 중요 운영 규칙을 추가
- [x] UnifiedVariableGraph Layer 1 contract를 정의하고 `ElementRegistry`로부터 occurrence-first 그래프를 빌드하며, host-range rebasing과 cross-artifact 변수 통합을 회귀 테스트로 고정
- [x] cbs-lsp Layer 3 `VariableFlowService`를 추가해 `UnifiedVariableGraph`와 on-demand variable-flow 결과를 변수별 readers/writers/issues/position query API로 묶고 회귀 테스트·체크리스트 Evidence·README를 함께 갱신
- [x] cbs-lsp deferred definition/references/rename/hover provider가 `VariableFlowService`를 optional Layer 3 input으로 받아 local-first 결과 뒤에 cross-file writer/reader와 hover summary를 병합하고, multi-file rename edit set·회귀 테스트·checklist Evidence·README를 함께 갱신

### Done (2026-04-20)

- [x] cbs-lsp `.risulua` LuaLS document/session routing foundation을 추가해 workspace scan과 standalone open/change/close가 `risu-luals` virtual Lua document mirror로 들어가도록 연결하고, queued didOpen/didChange/didClose transport·unit/integration test·checklist Evidence·README를 함께 갱신
- [x] cbs-lsp LuaLS sidecar foundation을 추가해 executable discovery(override→PATH), stdio spawn/initialize/shutdown, crash·health runtime 상태를 server lifecycle과 availability/trace payload에 연결하고, always-on fake contract test와 opt-in real-binary smoke test를 분리
- [x] cbs-lsp Layer 3 cross-file diagnostics refresh 경로를 추가해 writer/reader 관계 변경 시 관련 파일 diagnostics까지 다시 publish하고, workspace writer/reader가 있으면 local-only `CBS101`/`CBS102`를 억제하도록 서버/라우터·회귀 테스트·체크리스트·README를 함께 갱신
- [x] cbs-lsp Layer 3 `ActivationChainService`를 추가해 canonical lorebook text 기반 activation edge / partial match / cycle summary / URI·offset query surface를 제공하고, registry text snapshot·회귀 테스트·체크리스트·README를 함께 갱신
- [x] cbs-lsp lorebook CodeLens provider를 추가해 `ActivationChainService`의 possible incoming/outgoing count를 editor summary로 노출하고, partial/blocked/cycle 정책을 보조 CodeLens·회귀 테스트·README·체크리스트에 함께 고정
- [x] cbs-lsp workspace watched-file refresh 경로를 추가해 lorebook add/change/delete와 외부 workspace 변경이 기존 graph rebuild 경로로 들어오도록 연결하고, 영향받은 diagnostics republish + `workspace/codeLens/refresh` 요청을 서버 trace·README·체크리스트와 함께 정렬
- [x] cbs-lsp checklist의 blocked 항목을 코드 기준으로 재평가해 patch semantics/edit-model 두 항목은 blocker 유지, builtin metadata 확장 항목은 future로 상태를 바로잡음
- [x] cbs-lsp local-first definition provider를 server capability와 `textDocument/definition` handler에 연결하고, availability/trace/README wording·integration test·checklist Evidence를 코드 상태와 맞게 갱신
- [x] cbs-lsp local-first references provider를 server capability와 `textDocument/references` handler에 연결하고, availability/trace/README wording·unit/integration test·checklist Evidence를 코드 상태와 맞게 갱신
- [x] cbs-lsp local-first rename provider를 server capability와 `textDocument/prepareRename`/`textDocument/rename` handler에 연결하고, malformed/unresolved/external rejection UX·host range remap·README·체크리스트 Evidence를 코드 상태와 맞게 갱신
- [x] cbs-lsp definition/references/rename의 local-first merge contract를 공통 모듈로 정리해 cursor 해석·URI/range dedupe·stable ordering·rename edit 병합 규칙을 공유하고, unit/integration test·README·checklist Evidence를 함께 갱신
- [x] cbs-lsp shared `host-fragment-patch` contract를 추가해 fragment-local edit의 host safety boundary / same-URI multi-fragment 허용 창 / malformed no-op 규칙을 고정하고, formatting·rename·server wiring·회귀 테스트·README·checklist Evidence를 함께 갱신
- [x] cbs-lsp formatting provider를 실제 `textDocument/formatting` capability로 연결하고 CBS AST canonical formatter + host-fragment patch 승격 경로를 추가해 fragment-only/multi-fragment/malformed/unsupported no-op 정책을 테스트·체크리스트·README와 함께 갱신
- [x] cbs-lsp code action provider를 실제 `textDocument/codeAction` capability로 연결하고 diagnostics metadata + shared `host-fragment-patch` validator를 재사용해 quick fix/guide action을 구현한 뒤 회귀 테스트·README·checklist Evidence를 함께 갱신
- [x] cbs-lsp code action 결과에 normalized snapshot helper를 추가해 title/kind/linked diagnostics/edit/no-op guidance shape를 고정하고, feature/integration 테스트·체크리스트 Evidence·README를 함께 갱신
- [x] cbs-lsp document symbol provider를 실제 `textDocument/documentSymbol` capability로 연결하고 routed CBS fragment의 top-level block/`#func` outline + multi-fragment section container를 구현한 뒤 feature/integration 테스트·체크리스트 Evidence·README를 함께 갱신
- [x] cbs-lsp shared document-state cache 회귀 테스트를 추가해 completion/hover/folding/document symbol이 같은 URI/version `FragmentAnalysisService` 분석을 재사용하고, version 변경 시 새 분석으로 전환되는 계약을 checklist Evidence·README와 함께 갱신
- [x] cbs-lsp multi-provider normalized snapshot bundle helper를 추가해 completion/hover/diagnostics/code action 결과의 deterministic serialization을 고정하고, same-version text change 뒤 stale bundle 대신 새 분석 기준 bundle로 교체되는 회귀 테스트·checklist Evidence·README를 함께 갱신
- [x] cbs-lsp Layer 1 `IncrementalRebuilder`를 추가해 changed URI만 다시 읽고 `ElementRegistry`/`UnifiedVariableGraph`를 부분 갱신하도록 만들고, server open/change/close/watched-file 경로가 full workspace rebuild 대신 incremental refresh를 사용하도록 회귀 테스트·체크리스트·README를 함께 갱신
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 Lua bridge 5.3/5.4 섹션을 process transport / stub injection / read-only bridge / scope honesty 기준의 확실한 작업 단위로 세분화
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 Lua bridge 관련 항목에 LuaLS 미설치 기본 계약 테스트 / LuaLS 설치 후 실제 왕복 integration 테스트의 2층 테스트 전략을 명시
- [x] `packages/cbs-lsp/README.md`의 Lua Provider 설명에 사용자 설치 LuaLS 전략, unavailable fallback, pre/post-install 테스트 레이어 원칙을 반영
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`에 agent-facing workspace/query contract 전용 4.4 섹션을 추가하고 Layer 1 snapshot, Layer 3 query envelope, 남은 provider snapshot, schema version, runtime availability query 항목에 prerequisite과 touchpoint를 명시
- [x] cbs-lsp LuaLS hover proxy seam을 추가해 기존 `.risulua` virtual document mirror를 재사용한 `textDocument/hover` roundtrip을 서버에 연결하고, availability/trace/README/checklist/test layering을 hover-live·diagnostics/completion-deferred 범위로 정렬
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`에 독립 배포형 공용 LSP 승격을 위한 5.7 productization 섹션을 추가하고 standalone executable, 운영 계약, agent protocol hardening, LuaLS companion, VS Code client 경계, MCP/CLI adapter, 제품 수준 테스트, 릴리스, 문서 항목을 prerequisite·current gap·touchpoint와 함께 정리
- [x] subagent 조사 결과를 반영해 `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 미완료 Lua bridge / editor integration / test tracking 항목을 extension-first 서술에서 standalone/common LSP + 공식 VS Code client 관점으로 재작성
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 `5.7.1 Standalone executable / package contract`를 새 파일 없이 인라인 실행 단위(5.7.1-a~e)로 분해해 standalone CLI baseline, package surface, smoke test, README contract, config precedence 후속 단계를 구분
- [x] cbs-lsp standalone CLI baseline을 추가해 `cbs-language-server --help/--version/--stdio`와 explicit `bin`/`exports` package surface를 고정하고, build-backed smoke test·README usage·checklist Evidence를 함께 갱신
- [x] cbs-lsp shared runtime-config loader를 추가해 `CLI flag > env > config file > initialize option` precedence를 `--log-level`/`--workspace`/`--config`/`--luals-path`와 `initializationOptions.cbs`에 공통 적용하고, tracing·LuaLS startup·README·checklist Evidence를 함께 갱신
- [x] cbs-lsp `server.ts`의 `registerServer` orchestration을 lifecycle/feature/document/workspace helper로 분리하고, top-level helper 함수들에 JSDoc을 보강해 파일 진입 비용을 낮춤
- [x] cbs-lsp `server-tracing.ts`의 trace/log utility 타입과 log-level gate/serializer helper에 JSDoc을 추가해 tracing 모듈의 문서 밀도를 `server.ts`와 맞춤
- [x] cbs-lsp `server.ts`의 LSP/document/workspace callback 시작부에 한 줄 흐름 요약을 추가하고, 상위 registration helper JSDoc에 Flow 설명을 보강해 callback intent를 빠르게 읽을 수 있게 정리
- [x] cbs-lsp feature callback registrar를 `src/helpers/server-helper.ts`로 분리해 `server.ts`는 wiring 중심으로 줄이고, stateful helper class를 파일 단위 seam으로 승격함
- [x] cbs-lsp request/result semantic trace wrapper를 `server-tracing.ts`로 승격해 tracing 의미 계층을 호출부 helper가 아니라 tracing 모듈에서 제공하도록 정리
- [x] cbs-lsp duplicate utility 함수를 전수 정리해 `getFilePathFromUri`/`resolveWorkspaceRootFromFilePath`를 `helpers/path-helper.ts`, `formatOrdinal`/`formatRangeStart`/range text extraction을 `helpers/text-helper.ts`로 통합하고 관련 provider/server/indexer 호출부를 공용 helper 사용으로 정렬
- [x] cbs-lsp `refreshWorkspaceStateForUris`에 previousState/nextState의 before/after snapshot 의도와 pre/post rebuild 영향 URI 수집 이유를 주석으로 보강해 incremental refresh 흐름을 더 직접적으로 설명
- [x] cbs-lsp `refreshWorkspaceStateForUris`의 `if (nextState)` 분기 바로 위에 "새 snapshot 설치 vs stale state 정리" 의미를 직접 설명하는 주석을 추가해 branch intent를 코드 위치에서 바로 읽히게 정리
- [x] cbs-lsp `createRequestResolver` thin wrapper를 제거하고 `createServerFeatureProviders` 안에서 `resolveRequest`를 직접 정의해 `createFragmentRequest`와의 불필요한 2단 분리를 줄임

### Done (2026-04-21)

- [x] cbs-lsp workspace state ownership과 LuaLS/CodeLens orchestration을 `WorkspaceStateRepository`, `WorkspaceRefreshController`, `CodeLensRefreshScheduler`, `LuaLsCompanionController`로 이동하고 `server.ts`를 wiring 중심으로 다시 정리한 뒤 기존 integration test / build를 통과시킴
- [x] cbs-lsp Phase 1 refactor slice로 `src/server/capabilities.ts`, `src/server/commands.ts`, `src/controllers/DiagnosticsPublisher.ts`, `src/handlers/RequestHandlerRunner.ts`를 추가하고 `server.ts`/`server-helper.ts` wiring을 정리해 capability honesty(rename/codeAction/executeCommand), diagnostics version gating, request trace/cancellation 공통화를 회귀 테스트와 함께 고정
- [x] cbs-lsp `src/server.ts` 최상단 파일 개요 JSDoc에 `cli.ts -> startServer() -> registerServer() -> connection.listen()` 진입 체인을 명시해 파일 진입 비용을 낮춤
- [x] cbs-lsp `src/helpers/server-workspace-helper.ts`를 추가해 workspace client capability, document/request 복원, workspace diagnostics state 생성 유틸을 분리하고 `src/server.ts`를 orchestration 중심으로 정리
- [x] cbs-lsp `src/document-router.ts`의 파일 개요/함수 JSDoc을 저장소 주석 규약에 맞춰 정리하고 상수 설명 톤을 인접 helper 파일 밀도에 맞춤
- [x] cbs-lsp `src/utils/position.ts`와 `src/utils/diagnostics-router.ts`에 파일 개요 JSDoc과 exported/internal helper 주석을 보강해 utils 계층의 진입 설명 밀도를 helper 파일 수준으로 맞춤
- [x] cbs-lsp `src/analyzer/diagnostics.ts`에 파일 개요 JSDoc과 exported/public diagnostics helper 주석을 추가해 analyzer 진입 비용을 인접 cbs-lsp 모듈 밀도에 맞춤
- [x] cbs-lsp `src/analyzer/diagnostics.ts`의 `extractBlockNameRange`에도 내부 helper JSDoc을 추가해 비공개 range 계산 의도를 코드 위치에서 바로 읽히게 정리
- [x] cbs-lsp diagnostics stack을 `src/analyzer/diagnostics/` 하위 taxonomy / diagnostic-info / quick-fix / compare / block-header / pure-mode-filter / symbol / calc / collectors / engine 레이어로 분리하고, 기존 `src/analyzer/diagnostics.ts`는 안정적인 public barrel로 유지해 코드 액션·router·scope analyzer·fragment analysis import surface를 보존
- [x] cbs-lsp `src/analyzer/scopeAnalyzer.ts`에 파일 개요 JSDoc과 exported/private scope helper 주석을 추가해 loop/function scope 분석 흐름의 진입 비용을 낮춤
- [x] cbs-lsp scope analyzer를 `block-header/`와 `scope/` 하위 모듈로 분해해 shared header parser, analyzable body resolver, definition/reference collector, scope frame/rule helper를 분리하고 nested `#each` function scope·orphan `slot`·trimmed argument range 회귀 테스트를 추가
- [x] cbs-lsp `src/analyzer/symbolTable.ts`에 파일 개요 JSDoc과 `SymbolTable` public/private 메서드 주석을 추가해 심볼 등록·참조 조회 흐름의 진입 비용을 낮춤
- [x] cbs-lsp scope analysis result를 `SymbolTable` + `ScopeIssueStore` 경계로 분리하고 readonly symbol view, reference range dedupe, canonical local-function parameter 정책을 적용한 뒤 analyzer/diagnostics/provider tests를 갱신
- [x] cbs-lsp `src/config/runtime-config.ts`에 internal helper JSDoc과 precedence/discovery 흐름 주석을 추가해 standalone runtime config 해석 진입 비용을 낮춤
- [x] cbs-lsp `src/config/runtime-config.ts`를 bootstrap(config path) + runtime field resolver 단계로 재구성하고, CLI/env bootstrap 값과 runtime layer를 분리한 뒤 `resolveField()`로 value/source를 함께 결정하도록 정리했으며 explicit clear/null precedence·config parse error 회귀 테스트를 유지
- [x] cbs-lsp runtime/operator UX contract를 추가해 `experimental.cbs.operator`와 trace `availability.operator`가 install mode/PATH expectation/workspace root selection/multi-root·watched-file·LuaLS failure mode를 같은 shape로 노출하도록 정리하고 standalone usage·troubleshooting 문서, 회귀 테스트, checklist Evidence, README를 함께 갱신
- [x] cbs-lsp compatibility matrix를 추가해 Node/LuaLS/workspace shape별 supported/degraded/unsupported 범위를 `docs/COMPATIBILITY.md`, package manifest `engines/files`, runtime operator docs contract, README, 회귀 테스트, checklist Evidence에 함께 고정

### Done (2026-04-22)

- [x] cbs-lsp completion capability에 공용 `CBS_COMPLETION_TRIGGER_CHARACTERS` source-of-truth를 추가해 initialize response가 `['{', ':', '#', '/', '?', '<', '"']`를 광고하도록 정렬하고, CBS macro/block/calc trigger 회귀 테스트와 `.risulua` `getState("...")` opening-quote trigger completion roundtrip을 함께 고정
- [x] cbs-lsp initialize capability에 `positionEncoding: 'utf-16'`를 명시하고 `position.ts`/`COMPATIBILITY.md`에 UTF-16 code unit 계약과 negotiation 범위를 문서화했으며, 한글+이모지 혼합 문서에서 fragment remap/diagnostics/hover/rename/semantic token range 회귀 테스트를 추가함
- [x] cbs-lsp read-only Lua bridge MVP로 `getState("...")` / `setState("...")` 문자열 인수 위치에 VariableGraph 기반 상태 key overlay completion과 최소 response merger seam(`packages/cbs-lsp/src/providers/lua/responseMerger.ts`)을 추가하고, LuaLS proxy server path·unit/integration test·CBS checklist Evidence를 함께 갱신
- [x] cbs-lsp minimal RisuAI Lua stub MVP를 추가해 `packages/cbs-lsp/src/providers/lua/typeStubs.ts`에서 `RISUAI_API` 기반 `risu-runtime.lua`를 생성하고, `LuaLsCompanionController`가 start/restart/workspace refresh 때 generated stub path를 `workspace.library`에 자동 주입하도록 정리했으며, provider/controller 테스트·README·LuaLS companion 문서·CBS checklist를 함께 갱신하고 `vitest` 대상 테스트 + `npm run build`를 통과시켰음
- [x] cbs-lsp LuaLS shadow-file workspace vertical slice를 구현해 `.risulua` source 문서를 temp/shadow root 아래 canonical `.lua` mirror로 관리하고 LuaLS didOpen/didChange/didClose를 `file://` URI 기준으로 흘리며, incoming diagnostics를 다시 host `.risulua` URI로 remap하도록 전환. `lualsShadowWorkspace.ts` 모듈 추가, `lualsProcess.ts`의 initialize rootUri/workspaceFolders를 shadow root로 전환, 관련 8개 테스트를 shadow file:// URI 기반으로 갱신. 441개 테스트 통과.

- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.4.1 아래에 real LuaLS diagnostics 후속 작업을 놓치지 않도록 on-disk shadow `.lua` mirror, `Lua.workspace.library`/config injection seam, real-binary diagnostics smoke 복구 항목을 각각 별도 future 체크박스로 추가했음
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 real LuaLS 관련 항목을 재점검하면서 5.7.3의 오래된 `lua-diagnostics deferred` 서술을 현재 구현 상태(local-only diagnostics contract / real-binary diagnostics는 5.4.1 future)와 맞게 바로잡았음
- [x] local pinned LuaLS 3.18.2를 `.tools/lua-language-server/3.18.2`에 설치해 opt-in real-binary hover smoke를 실제로 돌릴 수 있게 했고, LuaLS diagnostics scheme 설정 키를 upstream 기준 `Lua.diagnostics.enableScheme`로 바로잡은 뒤 real hover smoke는 녹색으로 유지하면서 real diagnostics roundtrip은 아직 future 경계라는 점을 checklist/doc에 정직하게 고정
- [x] cbs-lsp LuaLS diagnostics vertical slice를 구현해 mirrored `risu-luals://` `textDocument/publishDiagnostics` notification을 host `.risulua` `publishDiagnostics`로 승격하고, unavailable/crashed/shutdown 시 stale Lua diagnostics clear·availability wording·always-on fake transport/integration tests를 함께 고정
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 4.4.1~4.4.5를 현재 코드베이스 기준으로 재판단해 future 일괄 상태를 partial/done 혼합 상태로 바로잡고, helper/CLI는 이미 있는 항목과 아직 contract 승격이 남은 항목을 분리해 보완했음
- [x] cbs-lsp stdio LSP 세션에 custom request `cbs/runtimeAvailability`를 추가해 runtime/operator availability snapshot을 CLI `report availability`와 같은 normalized shape로 다시 조회할 수 있게 하고, current-session LuaLS 상태·trace·integration test·README/agent guide·checklist Evidence를 함께 갱신
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.4.1 Lua diagnostics vertical slice를 without-LuaLS always-on contract 단계와 with-LuaLS opt-in real-binary 검증 단계로 분리해 다음 작업 단위를 더 직접적으로 만들었음
- [x] cbs-lsp 제품 문서 세트를 분리해 README를 quick-entry 중심으로 줄이고 standalone usage / agent integration / LuaLS companion / troubleshooting / VS Code client 가이드를 각각 독립 문서로 정리한 뒤 runtime operator docs contract·회귀 테스트·checklist Evidence를 함께 갱신
- [x] cbs-lsp standalone CLI에 `report`/`query` JSON adapter를 추가해 runtime availability, Layer 1 snapshot, Layer 3 variable/activation query surface를 LSP 밖에서도 소비할 수 있게 하고, fixture corpus helper·standalone CLI tests·README·checklist를 같은 auxiliary contract로 정렬
- [x] cbs-lsp agent-facing protocol hardening으로 completion/hover metadata, diagnostics/code-action availability envelope, Layer 1 `ElementRegistry`/`UnifiedVariableGraph` snapshot, Layer 3 `VariableFlowService`/`ActivationChainService` query result에 공통 `schema`/`schemaVersion` marker를 추가하고, fixture helper·README·회귀 테스트를 함께 갱신
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.7.3 남은 agent-facing protocol hardening 항목을 document symbol / CodeLens / future Lua provider 단위로 분해하고, 각 항목에 current gap·touchpoints·완료 기준을 붙여 다음 작업 단위를 명확히 정리
- [x] cbs-lsp document symbol provider에 normalized snapshot helper와 availability/provenance envelope를 추가해 `symbolKind` / `fragmentContainer` / `selectionRange` / `section` grouping contract를 fixture·feature test·integration test·README·checklist에서 함께 고정
- [x] cbs-lsp CodeLens provider에 normalized snapshot helper와 availability/provenance envelope를 추가해 summary/detail lens의 command mode, count policy, cycle count, refresh trigger semantics를 fixture·feature test·integration test·README·checklist에서 함께 고정
- [x] cbs-lsp Lua hover proxy seam에 normalized snapshot helper와 availability/provenance envelope를 추가해 live hover content와 `luaHover`/`lua-completion`/`lua-diagnostics` deferred boundary, LuaLS `ready`/`unavailable` runtime 상태를 provider·integration test·fixture helper·README·checklist에서 같은 contract로 고정
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.7.4 LuaLS companion 항목을 refactored controller 구조 기준으로 재평가해 `server.ts` 중심 서술을 `LuaLsCompanionController` / `WorkspaceRefreshController` / `server-helper` seam 기준으로 고쳐 쓰고, 이미 구현된 lifecycle/routing/hover foundation과 아직 남은 restart policy·후속 Lua surface gap을 분리해 정리
- [x] cbs-lsp LuaLS companion subsystem에 bounded auto-restart policy와 controller-level status seam을 추가하고 `.risulua` completion proxy를 hover와 같은 mirrored Lua seam으로 노출해 availability contract·README/docs·checklist·회귀 테스트를 함께 갱신
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.7.5 VS Code client boundary 항목을 현재 refactored product surface 기준으로 재작성해, 이미 완료된 standalone CLI/runtime operator 계약과 아직 남아 있는 VS Code dual-mode client·standalone-aware verify gap을 분리해 정리
- [x] `packages/vscode` CBS language client에 standalone-first dual-mode launch resolver(`auto`/`embedded`/`standalone`, `local-devDependency`/`npx`/`global`, explicit path override)를 추가하고, resolution failure UX·verify matrix·README/standalone usage/compatibility 문서를 함께 갱신
- [x] `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`의 5.7.6 Auxiliary surfaces beyond LSP 항목을 refactored codebase 기준으로 재평가해, 내부 Layer 1/3 query seam과 fixture snapshot helper는 반영하되 MCP/CLI query adapter 부재는 그대로 future gap으로 정리
- [x] cbs-lsp product-level test matrix를 추가해 built stdio server 외부 client lifecycle, real extracted workspace query/availability E2E, large workspace regression budget, opt-in real LuaLS stdio hover matrix를 `test:product-matrix`/`test:product-matrix:luals`와 checklist Evidence로 고정하고, large JSON report가 잘리던 CLI 종료 경로를 `process.exitCode` 기반으로 복구
- [x] cbs-lsp release/distribution pipeline을 추가해 Changesets 기반 semver/changelog policy, public-vs-private package strategy, CI verify workflow, stable/`next`/`canary` publish workflow, post-publish npm reinstall smoke test를 root scripts·package manifests·README·checklist와 함께 고정
- [x] cbs-lsp Layer 1 agent-facing snapshot contract를 `contract` descriptor로 승격해 `report layer1`/fixture helper가 stable fields·deterministic ordering·direct trust boundary를 machine-readable하게 함께 노출하고, README·agent guide·standalone/E2E/indexer tests·checklist Evidence를 같은 source-of-truth로 정렬
- [x] cbs-lsp Layer 3 query envelope contract를 `contract` descriptor로 승격해 `snapshotLayer3Queries()`/CLI `query` surface가 optional field/nullability, empty-array guarantee, deterministic ordering, intended consumer(agent/cli/mcp/tests)를 machine-readable하게 함께 노출하고, README·service/fixture/standalone/E2E tests·checklist Evidence를 같은 wording으로 정렬
- [x] cbs-lsp LuaLS workspace/library reinjection vertical slice를 구현해 `lualsWorkspace.ts` helper에서 `Lua.diagnostics.enableScheme` + `Lua.workspace.library` payload를 공용화하고, initialize/restart/workspace refresh 뒤에도 shadow root와 future stub root seam이 같은 `workspace/didChangeConfiguration` 경로로 다시 주입되도록 정리했으며, provider/helper/integration tests와 LuaLS companion 문서를 함께 갱신
- [x] cbs-lsp real-binary diagnostics smoke를 복구해 `tests/providers/luals-integration.test.ts`와 `tests/standalone/stdio-server.test.ts`에 non-empty diagnostics assertion을 추가하고, `docs/LUALS_COMPANION.md`에 execution conditions/recovery 경로를, `docs/TROUBLESHOOTING.md`에 `luals-diagnostics-empty` failure mode를, `checklist/CBS_CHECKLIST.md` 5.4.1 항목을 완료 처리함. `diagnostic-taxonomy.test.ts`의 deferred scope expectation에서 제거된 `lua-diagnostics` 항목을 정리해 466개 테스트 통과.
- [x] cbs-lsp RisuAI stub quality MVP를 완료해 `LuaLsCompanionController`가 generated `risu-runtime.lua`를 `workspace.library`에 재주입하는 것에 더해 shadow workspace mirrored document로도 유지하도록 보강했고, opt-in real LuaLS integration test에서 `getState(id, name)` hover와 `getState(...)` / `getLoreBooks(...)` completion label이 실제로 개선되는지 고정했으며 checklist/README/LuaLS companion 문서를 최신 상태로 갱신함
- [x] cbs-lsp read-only Lua bridge hover MVP를 추가해 `.risulua` `getState("x")` / `setState("x")` key 문자열 hover가 LuaLS 원본 hover 위에 VariableGraph 기반 cross-language bridge 요약을 병합하도록 만들고, 대표 Lua/CBS writer summary·workspace issue summary·overlay-only fallback을 `responseMerger`/server hover seam·provider/integration tests·CBS checklist Evidence에 함께 고정함
- [x] cbs-lsp scope honesty MVP를 마무리해 `experimental.cbs.operator.scope` / trace `availability.operator.scope` / deferred feature availability에 `read-only bridge: on`, `multi-file edit: off` 계약과 cross-language rename/workspace edit/code action off 상태를 함께 노출하고, availability/integration tests·README·audit checklist·CBS checklist Evidence를 최신 코드 상태에 맞게 갱신함

### Done (2026-04-23)

- [x] cbs-lsp stdio transport edge-case product smoke를 추가해 chunked Content-Length frame, single-write batched message, malformed JSON 뒤 safe shutdown/optional parse error 경로를 `tests/standalone/stdio-server.test.ts`로 고정하고 checklist Evidence를 함께 갱신
- [x] cbs-lsp codeAction lazy-resolve contract를 다시 정리해 unresolved payload는 경량(`source`/`relatedInformation` + minimal replacement metadata)으로 유지하면서도 server `codeActionResolve`가 replacement/close-tag/guidance action을 현재 문서 분석에서 안정적으로 복원하도록 수정하고, stale integration snapshot expectation을 현재 normalized code action shape에 맞게 갱신해 전체 `cbs-language-server` test를 복구함
- [x] cbs-lsp `completionItem/resolve`와 `codeAction/resolve`를 도입해 initialize capability가 `resolveProvider: true`를 광고하고, 서버 helper가 `onCompletionResolve`/`onCodeActionResolve`를 등록하도록 연결함. `CompletionProvider`는 `provideUnresolved()`로 label/kind/insertText/sortText만 eager하게 보내고 `resolve()`에서 detail/documentation/agent-metadata를 복원하며, `CodeActionProvider`는 `provideUnresolved()`로 title/kind/diagnostics만 eager하게 보내고 `resolve()`에서 edit payload를 복원함. large workspace benchmark에서 unresolved payload가 resolved payload보다 2배 이상 작고 resolve가 deferred field를 안정적으로 복원하는 회귀 테스트·체크리스트 Evidence·README를 함께 갱신함
- [x] cbs-lsp semantic tokens range provider를 추가해 initialize capability와 server handler가 `textDocument/semanticTokens/range`를 광고/등록하도록 연결하고, provider가 visible host range와 교차하는 fragment만 대상으로 full 분류 파이프라인을 재사용해 subset을 반환하도록 정리했으며, semantic token unit/integration/feature-matrix 회귀 테스트와 perf guard touchpoint를 함께 갱신함
- [x] cbs-lsp document highlight provider를 추가해 `getvar`/`setvar`, local `#func`/`call::name`, `arg::N`, `slot::alias`의 fragment-local read/write occurrence를 `textDocument/documentHighlight`로 즉시 강조하고, malformed/non-CBS/multi-fragment safe no-op 회귀 테스트와 capability/server wiring을 함께 고정함
- [x] cbs-lsp formatting golden contract helper를 추가해 single-fragment / multi-fragment / pure-mode / malformed / unicode fixture에서 idempotency와 fragment 바깥 host text 불변 조건을 공통 검증하고, host-fragment patch 회귀 테스트와 CBS checklist Evidence를 함께 갱신함
- [x] cbs-lsp formatting contract를 canonical serializer / pretty formatter / safe no-op 세 범주로 문서화하고, pure block / malformed fragment / multi-fragment host / unsupported artifact 정책을 README·COMPATIBILITY·formatting 회귀 테스트·CBS checklist에 같은 wording으로 고정함
- [x] cbs-lsp workspace snapshot freshness contract를 추가해 `WorkspaceStateRepository`가 rebuild마다 snapshot version을 승격하고 hover/completion provider metadata에 freshness marker를 싣도록 정리했으며, stale snapshot일 때 cross-file workspace 결과를 local-only로 degrade하는 회귀 테스트와 build 검증을 함께 고정함
- [x] cbs-lsp CBS completion에 workspace-aware chat-variable merge를 추가해 `getvar` / `setvar` / expression `$var`가 fragment-local 후보를 먼저 유지한 뒤 workspace persistent chat variable graph 후보를 source/detail 구분과 함께 뒤에 붙이도록 정리했고, temp/global namespace 비혼합 회귀 테스트·server integration·checklist Evidence를 함께 갱신함
- [x] cbs-lsp initialize capability matrix fixture를 `tests/fixtures/capability-matrix.ts`로 분리하고, prepareRename/codeAction literal/watched-files/codeLens refresh/LuaLS runtime 조합별 advertised capability·experimental availability·trace snapshot을 `tests/capability-matrix.test.ts`로 고정했으며, 기존 `lsp-server-integration.test.ts` initialize assertion은 wiring 중심으로 정리함
- [x] `docs/core/README.md`, `docs/core/domains/analyze/README.md`, `docs/core/domains/analyze/lua-analysis.md`를 다듬어 core root index discoverability와 analyze/lua wording을 현재 코드·CLI 동작에 맞게 바로잡음
- [x] cbs-lsp inbound `workspace/didChangeConfiguration` reload path를 추가해 client 설정 변경 시 runtime config를 다시 해석하고 log level gate를 즉시 반영하도록 정리했으며, immutable diagnostics/formatting 옵션은 explicit guidance 로그로 남기고 LuaLS companion restart/reconfigure + tracked workspace diagnostics/CodeLens refresh까지 integration/unit test와 checklist Evidence로 함께 고정함
- [x] `docs/core/common/principles.md`, `docs/core/domains/analyze/README.md`, `docs/core/node/README.md`, `docs/core/targets/root-browser.md`, `docs/core/targets/node-entry.md`를 추가해 `packages/core` 문서 경계를 custom-extension 스타일의 common/target/subtree 인덱스로 정리하고, root browser entry / node entry / analyze subtree / node subtree의 현재 계약과 subagent 로드 순서를 코드·테스트 기준으로 고정함
- [x] `docs/core/README.md`, common/testing, targets/cli, domain/node/analyze leaf 문서와 상호 링크 통합을 마무리해 남아 있던 docs/core hub·leaf 문서 세트를 현재 코드 구조 기준으로 완성함
- [x] repo root graph workflow opt-in integration을 추가해 `scripts/graph/*` wrapper, root `.mcp.json`, root package scripts, `node:test` smoke test, `.gitignore` local artifact ignore 규칙을 함께 고정하고 graphify/code-review-graph 미설치 상태에서는 actionable setup guidance로 정직하게 실패하도록 정리했으며, 후속으로 `docs/graphify-code-review-graph.md`와 root `README.md`/`README_EN.md` 포인터를 구현 상태에 맞게 정렬함
- [x] cbs-lsp CodeLens command ownership을 server-owned no-op `executeCommandProvider` 정책으로 고정해 lorebook CodeLens payload, initialize capability advertisement, VS Code client boundary, README/checklist wording, feature/integration test assertions을 같은 contract로 정렬함
- [x] graph workflow wrapper 후속 수정을 적용해 graphify/code-review-graph delegated cwd 기본값을 저장소 루트로 고정하고, smoke test가 다른 caller cwd에서도 그 anchoring을 검증하며, env override stub launcher를 Windows 친화적으로 정리하고 `docs/graphify-code-review-graph.md` wording을 실제 동작과 다시 맞춤
- [x] cbs-lsp CBS hover server seam을 request-URI 기반 `VariableFlowService` factory로 보강해 `getvar::x`/`setvar::x` hover가 local 정의와 workspace writer/reader count, 대표 writer 위치, variable-flow issue summary를 함께 보여주도록 정리하고, local-only degrade·server integration 회귀 테스트·CBS checklist Evidence를 함께 갱신함
- [x] graphify 글로벌 설치 검증 중 upstream CLI가 subcommand-only shape인 경우를 확인해 저장소 wrapper가 `unknown command '.'`로 깨지지 않도록 호환성 preflight와 안내 문구를 추가하고, 실수로 주입된 repo-local graphify installer 흔적(`AGENTS.md`/`.opencode/`)은 제거함
- [x] cbs-lsp `textDocument/rangeFormatting` vertical slice를 추가해 선택 range가 정확히 하나의 CBS fragment 안에 있을 때만 owning fragment canonical edit를 반환하고, multi-fragment/non-CBS 교차 선택은 safe no-op으로 고정했으며 capability·server seam·feature/integration test·CBS checklist Evidence를 함께 갱신함
- [x] cbs-lsp `textDocument/onTypeFormatting` vertical slice를 추가해 newline trigger 기준 단일 안정 CBS fragment에서만 최소 indentation edit를 반환하고, multi-fragment/non-CBS/malformed/recovery 상태는 safe no-op으로 고정하며, capability·server seam·feature/integration test·trace type·CBS checklist Evidence를 함께 갱신함
- [x] cbs-lsp `textDocument/inlayHint` vertical slice를 추가해 `setvar/getvar/#when/#each/#func/call/arg`에 대한 fragment-safe parameter/block-header hints를 노출하고, server capability·feature/integration/capability-matrix 회귀 테스트·CBS checklist Evidence를 함께 갱신함
- [x] cbs-lsp `textDocument/selectionRange` vertical slice를 추가해 token span → macro whole → nearest parent block body → block whole chain을 현재 fragment 안에서만 확장하도록 구현하고, capability/server seam/trace feature name/feature·integration·capability-matrix 회귀 테스트와 CBS checklist Evidence·README를 함께 갱신함
- [x] cbs-lsp `workspace/symbol` vertical slice를 추가해 variables / CBS local `#func` / lorebook entries / prompt sections를 prefix·fuzzy query로 노출하고, deterministic ordering·artifact별 `SymbolKind` 정책·first-workspace-folder degraded contract를 feature/integration/capability-matrix 테스트·README·COMPATIBILITY·CBS checklist와 함께 고정함

- [x] `packages/cbs-lsp/docs/CBS_INLAY_HINT_REUSABLE_PATTERNS.md` 문서를 한국어로 번역하고, 인레이 힌트 구현을 위한 재사용 가능 패턴 및 아키텍처 가이드를 최신 상태로 현행화함

## Archived from `TODO.md` on 2026-04-27

### Done (2026-04-27)

- [x] `TODO.md`에 남아 있던 2026-04-24~2026-04-27 완료 이력을 `FIN.md`의 2026-04-27 아카이브 섹션으로 옮기고, `TODO.md`는 `### Remaining`/`#### Backlog`의 잔여 작업 4건만 담도록 정리함. AGENTS.md의 FIN 기록/TODO Remaining 정책과 현재 문서 상태를 대조하고, 문서 diff 및 TODO 구조 검증을 완료함
- [x] `packages/cbs-lsp/src/auxiliary/agent-contracts.ts`의 Layer 1/Layer 3 agent contract JSDoc을 전문적인 계약 문서 어조로 정리함. 파일 개요, descriptor/interface, stable field, nullability, ordering, snapshot helper 주석의 완곡한 서술을 짧고 직접적인 표현으로 다듬고 `@param` 설명 형식을 저장소 JSDoc 규칙에 맞게 통일했으며, 수정 파일 LSP diagnostics, Prettier check, cbs-lsp build 검증을 완료함
- [x] `.risulua` Trigger Suggest에서 RisuAI Lua runtime global 함수 후보를 LuaLS stub 의존 없이 CBS-LSP가 직접 overlay하도록 구현함. `packages/cbs-lsp/src/providers/lua/risuaiRuntimeOverlay.ts`에 core runtime signature/documentation catalog 기반 completion builder를 추가하고, `server-helper.ts`의 `.risulua` completion merge 경로가 LuaLS 후보와 dedupe한 runtime overlay를 state-key/CBS completion과 함께 병합하게 했으며 LuaLS unavailable/empty response/oversized `.risulua` 경계 회귀 테스트를 보강함. 수정 파일 LSP diagnostics, runtime overlay provider test, server-helper targeted test, `.risulua` completion integration test, cbs-lsp build 검증을 완료함
- [x] `.risulua` Trigger Suggest에서 RisuAI Lua runtime global 함수 후보를 LuaLS stub 의존 없이 CBS-LSP가 직접 overlay하기 위한 구현 계획을 작성함. `docs/superpowers/plans/2026-04-27-risulua-runtime-completion-overlay.md`에 runtime completion builder, `.risulua` completion routing merge, LuaLS/generated-stub dedupe, oversized LuaLS skip, provider/helper/integration 테스트, TODO 갱신과 검증 명령을 task-by-task checklist로 정리함
- [x] `_G.axLLM` 같은 Lua global table member access가 hover/go-to-definition은 runtime overlay로 연결되지만 LuaLS diagnostics에서 `Undefined field \`axLLM\`.Lua Diagnostics.(undefined-field)` underline을 남기던 문제를 수정함. `packages/core/src/domain/analyze/lua-type-stubs.ts`가 generated `risu-runtime.lua` 끝에 모든 RisuAI runtime global의 `_G.<name> = <name>` alias를 생성하게 해 LuaLS가 `_G` field를 known runtime member로 보도록 했고, core stub 회귀 테스트에 `_G.axLLM`/`_G.LLM`/`_G.getState`/`_G.json` alias 검증을 추가함. 수정 파일 LSP diagnostics, core runtime stub targeted test, core build, cbs-lsp runtime overlay/server-helper targeted tests, cbs-lsp build 검증을 완료함
- [x] `.risulua` runtime overlay의 hover tooltip과 generated `risu-runtime.lua` 주석이 `LLM`/`axLLM`에만 치우쳐 부실해 보이던 상태를 개선함. `packages/core/src/domain/analyze/lua-type-stubs.ts`의 runtime documentation catalog를 chat/state/lorebook/UI/async/hook/LLM/image/request/json/Promise 계열 주요 API 전반으로 확장하고, 아직 curated 문서가 없는 runtime global도 `RISUAI_API`의 category/access/direction 기반 fallback 설명을 받도록 해 go-to-definition target 전체가 최소 주석을 갖게 함. core/cbs-lsp hover·stub 회귀 테스트를 여러 API로 확장했고, 수정 파일 LSP diagnostics, core runtime stub targeted test, core build, cbs-lsp runtime overlay/server-helper targeted tests, cbs-lsp build 검증을 완료함
- [x] `.risulua` runtime overlay에서 `LLM`/`axLLM` hover tooltip과 generated `risu-runtime.lua` go-to-definition target 주석을 풍부하게 보강함. `packages/core/src/domain/analyze/lua-type-stubs.ts`에 `getRisuAiLuaRuntimeDocumentation()` catalog를 추가해 stub LuaDoc과 CBS-LSP hover가 같은 설명/파라미터/반환값/예제를 공유하게 했고, `packages/cbs-lsp/src/providers/lua/risuaiRuntimeOverlay.ts`는 core catalog를 markdown hover에 렌더링하도록 갱신함. 수정 파일 LSP diagnostics, core runtime stub targeted test, cbs-lsp runtime overlay/server-helper targeted tests, core/cbs-lsp build 검증을 완료함
- [x] `.risulua` runtime overlay가 `_G.axLLM`처럼 Lua global table을 통해 RisuAI 전용 runtime 함수를 확인하거나 `local invokeModel = axLLM`처럼 runtime 함수를 local alias에 할당하는 패턴도 hover/go-to-definition으로 연결되게 보강함. `packages/cbs-lsp/src/providers/lua/risuaiRuntimeOverlay.ts`가 `_G.<runtime>` member expression 전체에서 property runtime token을 인식하게 했고, provider/server-helper 테스트에 `_G.axLLM` 및 alias assignment RHS hover/definition 회귀를 추가함. 수정 파일 LSP diagnostics, runtime overlay/server-helper targeted tests, cbs-lsp build 검증을 완료함
- [x] `.risulua`에서 RisuAI Lua runtime global(`log`, `getState`, `LLM` 등)의 hover/go-to-definition을 LuaLS generated stub indexing에 의존하지 않는 CBS-LSP 직접 overlay로 제공함. `packages/core/src/domain/analyze/lua-type-stubs.ts`에 runtime signature catalog export를 추가하고, `packages/cbs-lsp/src/providers/lua/risuaiRuntimeOverlay.ts`가 bounded current-line token detection, RisuAI Runtime hover markdown, deterministic `risu-runtime.lua` definition target을 제공하게 했으며, `server-helper.ts` 라우팅은 CBS macro priority를 유지한 채 runtime overlay를 LuaLS fallback/oversized skip보다 먼저 확인하도록 보강함. core/cbs-lsp targeted tests, 수정 파일 LSP diagnostics, core/cbs-lsp build 검증을 완료함
- [x] `.risulua`에서 RisuAI Lua runtime API hover/go-to-definition을 LuaLS stub 의존 대신 CBS-LSP 직접 overlay로 제공하기 위한 구현 계획을 작성함. `docs/superpowers/plans/2026-04-27-risuai-lua-runtime-overlay.md`에 core signature export, `risuaiRuntimeOverlay.ts` 신규 provider, server-helper hover/definition routing, responseMerger merge 정책, oversized `.risulua`/CBS macro priority/LuaLS fallback edge case, targeted test/build 검증 명령을 task-by-task checklist로 정리함
- [x] `.risulua`를 `lua` language id로 여는 native sumneko.lua LuaLS가 RisuAI runtime API를 hover/go to definition 대상으로 설명하지 못하던 경계를 보완함. `packages/core/src/domain/analyze/lua-type-stubs.ts`로 RisuAI Lua runtime stub/globals 생성 source-of-truth를 core로 이동해 cbs-lsp와 VS Code client가 함께 쓰게 했고, `packages/vscode`에 `Risu Workbench: Generate LuaLS Stubs` 명령을 추가해 workspace-local `.vscode/risu-stubs/risu-runtime.lua`를 생성한 뒤 `Lua.workspace.library`에 중복 없이 연결하게 함. core/cbs-lsp/vscode helper와 manifest boundary tests를 보강했고 수정 파일 LSP diagnostics, core Lua stub targeted test, cbs-lsp Lua stub targeted tests, core/cbs-lsp build, VS Code boundary 25개 검증을 완료함
- [x] `.risulua`를 `lua` language id로 여는 native sumneko.lua LuaLS가 CBS LSP sidecar의 generated stub 설정을 공유받지 못해 `log` 같은 RisuAI runtime global을 `undefined-global`로 계속 진단하던 경계를 간단 해결함. `packages/vscode/package.json`의 `configurationDefaults`에 `Lua.diagnostics.globals` 기본값을 추가해 native LuaLS도 RisuAI injected API, wrapper helper, `json`/`Promise`, hook callback 전역을 known global로 보게 했고, VS Code boundary test가 `log`/`getState`/`LLM`/`listenEdit` 등 핵심 전역 기본값을 검증하도록 보강함. package.json/test LSP diagnostics, package.json JSON parse, VS Code boundary E2E 23개 검증을 완료함
- [x] `.risulua` LuaLS diagnostics가 upstream RisuAI runtime global인 `log` 등을 `undefined-global`로 오진하던 경계를 보완함. `docs/upstream-traceability/artifacts/lua.md`와 `docs/risuai-lua.d.ts`의 upstream `scriptings.ts#runScripted`/`declareAPI`/`luaCodeWrapper` 근거를 따라 `packages/cbs-lsp/src/providers/lua/typeStubs.ts`의 generated LuaLS stub을 기존 `getState`/`setState`/`getLoreBooks` 최소 목록에서 RisuAI injected API, wrapper helper, `json`/`Promise`, script hook callback 전역까지 확장하고, `lualsWorkspace.ts`의 `Lua.diagnostics.globals`에도 같은 deterministic 목록을 주입하게 함. 수정 파일 LSP diagnostics, LuaLS stub/workspace/process/controller targeted tests, cbs-lsp build 검증을 완료함
- [x] Risu custom extension 아이콘 적용 뒤 Extension Development Host에서 다른 확장/테마의 파일 아이콘이 사라지던 문제를 수정함. 원인은 `Risu Workbench Icons`가 부분 아이콘이 아니라 전체 VS Code File Icon Theme로 등록되어 활성 테마를 대체하던 구조였으므로, `packages/vscode/package.json`의 `contributes.iconThemes` 기여와 미사용 `resources/risu-file-icon-theme.json`을 제거하고 language-level `contributes.languages[].icon`만 유지하게 정리함. boundary test도 글로벌 file icon theme 미등록을 회귀 조건으로 갱신함
- [x] `packages/vscode/resources/icon/risulua.svg`가 같은 VS Code 파일 아이콘 세트와 어긋나던 문제를 다시 정리함. 실패했던 선형 `L`/orbit 시안은 제거하고, 기존 크림 문서 카드·갈색 stroke·오렌지/청록 포인트 팔레트를 유지한 채 Lua 로고의 핵심인 큰 원과 작은 위성 원만 단순 면 glyph로 재작성함. package.json LSP diagnostics, Python XML 기반 SVG parse smoke, VS Code build 검증을 완료함
- [x] `risuregex.svg` 파일 아이콘이 일반 코드 중괄호 아이콘처럼 보이던 문제를 줄이기 위해, 16px 파일 아이콘 안에서 정규식 의미가 더 직접적으로 읽히도록 `^` anchor, `[]` character class, teal `.*` wildcard 모티프 조합으로 재작성함. package.json/test LSP diagnostics, package.json/icon theme/SVG smoke parse, VS Code build, VS Code boundary test 23개 검증을 완료함
- [x] VS Code Explorer 파일 아이콘 패밀리를 Risu custom extension 전반으로 확장함. 기존 고밀도 일러스트였던 `risumodule.svg`, `risucharx.svg`, `risupreset.svg`, `risulorebook.svg`, `risuregex.svg`를 모두 `viewBox="0 0 16 16"` 파일 아이콘용 단순 glyph로 재작성하고, `risuhtml.svg`, `risulua.svg`, `risuprompt.svg`, `risuvar.svg`, `risutoggle.svg`를 신규 추가함. `packages/vscode/package.json`에는 `.risulorebook`/`.risuregex`/`.risuprompt`/`.risuhtml`/`.risulua`/`.risuvar`/`.risutoggle` language icon을 연결하고, `.risumodule`/`.risucharx`/`.risupreset`과 `*.risulua` file association 경계까지 잡을 수 있도록 `Risu Workbench Icons` 파일 아이콘 테마와 `resources/risu-file-icon-theme.json`을 추가했으며, icon contribution boundary test도 보강함. package.json/test LSP diagnostics, package.json/icon theme/SVG smoke parse, VS Code build, VS Code boundary test 23개 검증을 완료함

### Done (2026-04-26)

- [x] CBS completion이 `.risutoggle` 정의를 두 가지 이름공간으로 사용하도록 보강함. core toggle adapter에 risutoggle DSL의 `name=...` 행을 보존적으로 읽는 `parseToggleDefinitions()`를 추가해 원본 toggle name과 파생 `toggle_<name>` globalvar 이름을 계산하게 했고, cbs-lsp `VariableFlowService`가 workspace `.risutoggle` 파일을 completion summary로 인덱싱하게 함. `{{getglobalvar::toggle_...}}` 같은 global variable 인자 completion에는 `toggle_response_mode-gpt-5.4` 형태를 제안하고, `{{#when::toggle::...}}` 인자 completion에는 risutoggle 원본 key인 `response_mode-gpt-5.4` 형태를 제안하도록 `CompletionProvider`와 `#when` plain-text recovery context를 갱신함. 수정 파일 LSP diagnostics, core toggle canonical targeted test, cbs-lsp completion targeted test, core build, cbs-lsp build 검증을 완료함
- [x] `docs/upstream-traceability`에 RisuAI upstream CBS calculate/equality 처리 방식을 추가 추적하고, 이후 실제 upstream runtime 호환에 맞춰 single `=` 설명을 정정함. `domains/cbs-pipeline.md`에 upstream `parser.svelte.ts#matcher`가 `{{? ...}}`를 registry callback이 아닌 parser-level special-case로 `calcString()`에 보내고, `cbs.ts#calc`는 `{{calc::...}}` registered function이며 `cbs.ts#?`는 `doc_only` registry row라는 차이를 trace row로 기록함. 또한 `infunctions.ts#executeRPNCalculation/#toRPN/#calculateRPN`가 `== -> =`, `!= -> ≠`로 rewrite하고 missing-left operand에 `0`을 주입해 bare `=1`이 runtime에서는 `0 = 1`처럼 평가될 수 있으므로, workbench LSP diagnostics도 single `=` equality token을 허용해야 한다는 호환 boundary로 문서를 갱신함. `registries/upstream-anchors.md`에는 `parser.svelte.ts`와 `infunctions.ts`를 새 upstream spine anchor로 추가하고 patch watchpoint를 갱신함
- [x] CBS LSP calc expression 진단에서 upstream RisuAI calculate와 맞게 single `=`를 equality token으로 허용하도록 수정함. `packages/cbs-lsp/src/core/calc-expression.ts`의 operator documentation, tokenizer, binary operator set에 `=`를 추가하고, bare `{{? =1}}`/`{{calc::=1}}`는 upstream `toRPN()`의 missing-left operand `0` fallback과 맞게 진단 없이 통과하도록 했으며 사용자 재현 형태인 `{{#if_pure {{? !({{getglobalvar::...}}=1)}}}}`, `{{#if {{? {{getglobalvar::...}}=1 }}}}` block header 문맥도 `CalcExpressionUnsupportedToken(CBS011)` 없이 통과하는 회귀 테스트로 교체함. completion/semantic token/operator cheat-sheet도 `=` operator를 인식하도록 보강했고, 수정 파일 LSP diagnostics, core build 후 calc 관련 targeted tests, diagnostics-engine full test, cbs-lsp build 검증을 완료함
- [x] lorebook activation summary CodeLens가 단순 count만 담던 상태를 고도화해, `CodeLens.data.lens.activation`과 command arguments에 현재 엔트리를 활성화하는 possible incoming entry와 현재 엔트리가 활성화하는 possible outgoing entry의 이름·상대 경로·매칭 키워드·file-open command link metadata를 담도록 함. LSP 표준 CodeLens/Command title은 Markdown tooltip/link를 지원하지 않고 현재 languageclient 변환기도 server command tooltip을 전달하지 않으므로, 서버 snapshot에는 plainText/markdown 양쪽 metadata를 보존하고 공식 VS Code client middleware가 `ProtocolCodeLens.data`의 plainText를 실제 `vscode.Command.tooltip`로 복원하게 구현함. 이후 사용자의 실제 의도에 맞춰 CodeLens 클릭 시 QuickPick 팝업에서 “활성화하는 엔트리”와 “활성화시킨 엔트리”를 선택해 기존 `risuWorkbench.cbs.openOccurrence` file-only navigation guard로 이동하게 보강함. 런타임에서 `cbs-lsp.codelens.activationSummary`를 VS Code command로 직접 등록하면 languageclient가 server `executeCommandProvider` 광고 command를 자동 proxy 등록하며 중복 충돌하는 문제를 확인하고, server-owned no-op command id는 그대로 두되 실제 popup entry point는 client-owned `risuWorkbench.cbs.showActivationLinks`로 분리해 충돌을 닫음. Oracle 리뷰 후 middleware-level helper, malformed payload, command URI encoding, popup item 회귀 테스트를 보강했고, 수정 파일 LSP diagnostics, cbs-lsp CodeLens feature test, cbs-lsp build, VS Code build, VS Code client boundary 22개 검증을 완료했으며, 기존 `#### Backlog`의 lsp-server integration CodeLens/workspace-state 실패 2건은 같은 remain risk로 재확인함
- [x] 루트 VS Code 디버그 설정을 추가해 `packages` 하위 빌드 산출물을 만든 뒤 `packages/vscode`를 Extension Development Host로 실행할 수 있게 함. `.vscode/tasks.json`에 `wasm:build → build:core → build:cbs-lsp → build:vscode` 순차 빌드 태스크를 추가하고, `.vscode/launch.json`에서 해당 태스크를 `preLaunchTask`로 연결해 `${workspaceFolder}/packages/vscode`를 extension development path로 지정함. 신규 JSON 파일 LSP diagnostics, JSON parse, Prettier check, WASM/core/cbs-lsp/vscode 빌드 체인 검증을 완료함
- [x] `feature/rust-wasm-lua-analyzer` 브랜치를 현재 `feat/architecture-rework` 브랜치로 fast-forward merge함. 기존 작업 트리 변경사항은 stash로 보존한 뒤 복원했고, `.gitignore`/`TODO.md`/`package-lock.json` stash 충돌은 Rust/WASM 병합 결과와 로컬 작업물을 보존하는 방향으로 정리했으며 conflict marker와 package-lock JSON 유효성 검증을 완료함
- [x] 사용자가 기대한 “`.risulua`에서 LuaLS가 Lua와 동일하게 붙고 CBS LSP도 동시에 붙는” 동작을 실제 VS Code language id 기준으로 맞춤. 기존 기본 등록은 `.risulua -> risulua`라 Sumneko/Lua 확장이 native attach할 수 없고 CBS sidecar proxy만 우회로로 동작하던 구조였으므로, `package.json`의 `risulua` language contribution에서 `.risulua` extension 소유권을 제거하고 `configurationDefaults.files.associations`로 `*.risulua`를 `lua`에 기본 연결하게 변경함. CBS LSP는 이미 `{ scheme: 'file', pattern: '**/*.risulua' }`와 `{ language: 'lua', scheme: 'file', pattern: '**/*.risulua' }` selector가 있어 같은 파일에 계속 attach되며, Lua language 상태에서도 CBS TextMate highlighting이 유지되도록 `source.lua` injection grammar `risulua-cbs-injection.tmLanguage.json`을 추가함. VS Code boundary test를 native LuaLS attach 기본 association과 Lua CBS injection grammar 계약으로 갱신했고, VS Code build, boundary 19개, manifest/syntax JSON parse, cbs-lsp build 검증을 완료함
- [x] `.risulua`에서 LuaLS sidecar가 healthy여도 VS Code 체감상 CBS LSP만 붙은 것처럼 보이던 원인을 수정함. 기존 LuaLS proxy live surface가 hover/completion/definition/rename/diagnostics에만 묶여 있어 Lua function/variable의 document symbol, document highlight, references, signature help가 CBS provider 경로로만 처리되던 문제를 `LuaLsProxy`/`LuaLsCompanionController`/`ServerFeatureRegistrar`에서 확장해, `.risulua` Lua 코드 영역은 shadow `.lua` 문서의 LuaLS references/documentHighlight/documentSymbol/signatureHelp를 함께 사용하고 CBS macro 내부는 기존 CBS 즉시 응답 우선 정책을 유지하게 함. 또한 CBS server가 LuaLS sidecar initialize 때 빈 `capabilities: {}`를 보내던 경계를 보강해 hover/completion/definition/references/rename/signature/documentSymbol/documentHighlight/diagnostics/workspace configuration capability를 명시 전달하도록 바꿈. 수정 파일 LSP diagnostics, LuaLS proxy/process/controller/helper targeted tests 29개, cbs-lsp build 검증을 완료했으며, 전체 cbs-lsp suite는 기존 fragment-routing/workspace-state/release fixture 실패 24건이 남아 별도 remain risk로 재확인함
- [x] LuaLS merge 경로를 붙인 뒤 `.risulua`의 `{{getvar::ct_tempvar}}` 같은 CBS variable argument 위치에서 hover/go to definition이 LuaLS proxy 대기/merge 경로에 묶여 체감상 죽던 회귀를 수정함. `server-helper.ts`의 `.risulua` hover/definition routing에서 CBS provider 결과가 있고 cursor가 실제 `{{...}}` CBS macro 내부일 때는 LuaLS를 기다리지 않고 CBS 결과를 즉시 반환하게 좁혔고, macro 밖 Lua `getState("...")` 같은 Lua state bridge 문맥은 계속 LuaLS merge/fallback을 타도록 `isPositionInsideCbsMacro()` gate를 추가함. 회귀 테스트는 실제 사용자 형태인 `{{getvar::ct_tempvar}}`와 `variables/defaults.risuvar` default key를 사용하도록 갱신하고 async definition handler를 실제처럼 await하며 workspace refresh 이후 hover default value와 `.risuvar` definition target을 검증하게 보강함. 수정 파일 LSP diagnostics, `server-helper.test.ts` 9개, `lsp-server-integration.test.ts`의 `ct_tempvar` targeted test, cbs-lsp build 검증을 완료함
- [x] VS Code에서 `sumneko.lua` 확장이 이미 LuaLS를 실행 중이어도 CBS LSP sidecar는 별도 child process라 같은 LSP transport를 재사용할 수 없고, 기존 구현은 명시 `risuWorkbench.cbs.server.luaLsPath` 또는 PATH의 `lua-language-server`만 보던 문제를 보강함. VS Code client가 explicit `luaLsPath`가 비어 있을 때 `Lua.misc.executablePath` 설정과 설치된 `sumneko.lua` extension의 `server/bin/lua-language-server` 계열 경로를 자동 탐색해 `CBS_LSP_LUALS_PATH`로 넘기도록 했으며, 현재 환경에서 발견된 `/home/noel/.vscode-server/extensions/sumneko.lua-3.18.2-linux-x64/server/bin/lua-language-server` 같은 remote extension bundle 경로도 잡을 수 있게 함. verify script에 sumneko auto-discovery drift guard를 추가했고 수정 파일 LSP diagnostics, VS Code build, CBS client verify script, e2e boundary test 18개 검증을 완료함
- [x] `.risulua`를 `lua`로 file association했을 때 extension activation은 되지만 VS Code LanguageClient `documentSelector`가 `languageId=lua` 문서에 실제로 attach하지 않아 LuaLS hover/go to definition/rename/completion 요청 자체가 CBS LSP로 전달되지 않던 누락을 수정함. `CBS_DOCUMENT_SELECTORS`에 일반 Lua 전체가 아니라 `{ language: 'lua', scheme: 'file', pattern: '**/*.risulua' }` 필터를 추가해 `.risulua` 파일만 호환 attach되게 했고, boundary E2E와 verify script가 이 selector drift를 잡도록 보강함. 또한 `cbs/runtimeAvailability`를 client startup 후 조회해 Output channel에 LuaLS sidecar status/health/executable과 unavailable recovery를 출력하게 하여, LuaLS 미설치/PATH 누락/sidecar crash가 더 이상 조용한 실패로 보이지 않게 함. 수정 파일 LSP diagnostics, VS Code build, e2e boundary test 18개, CBS client verify script 검증을 완료함
- [x] VS Code에서 한 파일에 `lua`와 `risulua` language id를 동시에 부여할 수 없기 때문에, 사용자가 `files.associations`로 `*.risulua`를 `lua`로 연결해도 CBS client가 같이 살아나는 호환 경로를 추가함. 기본 `.risulua -> risulua` association은 유지하되 `onLanguage:lua`와 `workspaceContains:**/*.risulua` activation event를 추가해 Lua association 상태에서도 extension/CBS LanguageClient가 시작되게 했고, 기존 file-pattern document selector(`**/*.risulua`)가 LSP attach를 담당하게 유지함. 또한 client-side CBS auto suggest와 bracket pair decoration이 `languageId=lua`라도 파일명이 `.risulua`이면 CBS 문서로 취급하도록 보강했으며, activation/association/auto-suggest 회귀 테스트와 verify script drift guard를 추가하고 수정 파일 LSP diagnostics, VS Code build, e2e boundary test 18개, CBS client verify script 검증을 완료함
- [x] `.risulua` LuaLS sidecar 라우팅을 고쳤는데도 VS Code에서 hover/go to definition/rename이 계속 비활성처럼 보이던 런타임 원인을 추가 조사해, VS Code extension 설정에서 LuaLS executable path를 CBS server로 전달하는 경로가 없던 문제를 수정함. `risuWorkbench.cbs.server.luaLsPath` 설정을 `package.json`에 추가하고, `cbsLanguageClient.ts`가 이를 `CBS_LSP_LUALS_PATH` env로 standalone/embedded CBS server 프로세스에 전달하게 했으며, pure boundary snapshot도 같은 env forwarding 계약을 반영하도록 갱신함. Extension-host test settings, boundary E2E, verify script도 새 설정을 포함하도록 보강했고 VS Code build, CBS LSP build, VS Code boundary test 15개, CBS client verify script 검증을 완료함. 실제 Extension Development Host에서는 `cbs-lsp`/`vscode` rebuild 뒤 기존 server process가 새 코드를 자동으로 물지 않으므로 window reload 또는 Extension Host 재시작이 필요함
- [x] `.risulua`에서 LuaLS sidecar가 순수 Lua local function/variable에 대해 hover는 일부만, go to definition/rename은 전혀 제공하지 못하던 라우팅 문제를 수정함. `LuaLsProxy`에 `textDocument/definition`, `textDocument/prepareRename`, `textDocument/rename` 프록시를 추가하고 shadow `.lua` URI로 들어온 LuaLS 결과를 원본 `.risulua` URI로 되돌리게 했으며, `LuaLsCompanionController` subsystem status를 definition/rename live surface로 갱신함. `ServerFeatureRegistrar`는 `.risulua` definition을 CBS+LuaLS 병합으로 처리하고, rename은 CBS rename 불가 시 LuaLS rename으로 fallback하며, hover는 CBS 결과가 있어도 LuaLS hover를 함께 조회해 병합하도록 변경함. LuaLS proxy URI remap 테스트, server-helper definition/hover/rename routing 회귀 테스트, controller surface contract 테스트를 보강했고 수정 파일 LSP diagnostics, LuaLS provider/helper targeted tests, cbs-lsp build 검증을 완료함

### Done (2026-04-25)

- [x] CBS variable go to definition이 hover summary처럼 workspace writer뿐 아니라 reader 위치까지 함께 반환하도록 확장함. `DefinitionProvider`의 workspace target 병합 경로와 oversized `.risulua` fast path가 `VariableFlowService.queryVariable()`의 writers/readers를 모두 `LocationLink` target으로 변환하게 했고, availability contract wording도 writers/readers로 갱신함. definition feature test와 server seam integration test에 reader 포함 회귀를 추가했으며 수정 파일 LSP diagnostics, targeted definition test, definition integration test, availability snapshot tests, cbs-lsp build 검증을 완료함
- [x] CBS variable hover tooltip에 표시되는 external reader/writer(`.risulorebook`, `.risuregex`, `.risulua` 등) 위치가 텍스트로만 보이고 클릭해도 VS Code에서 정확한 위치로 이동하지 못하던 문제를 수정함. `HoverProvider`가 workspace occurrence 위치를 `file://...#line,char` 링크 대신 `command:risuWorkbench.cbs.openOccurrence?...` 링크로 렌더링하게 바꾸고, VS Code client는 trusted markdown을 해당 command 하나로만 제한해 허용하며 command handler가 local `file:` URI와 range를 검증한 뒤 `openTextDocument`/`showTextDocument`로 정확한 selection을 열도록 보강함. Oracle review에서 지적한 arbitrary URI scheme 리스크를 `file:` scheme guard로 닫았고, hover markdown command URI 회귀 테스트·VS Code command contribution boundary test·수정 파일 LSP diagnostics·hover/definition targeted test·cbs-lsp build·VS Code build·VS Code boundary test 검증을 완료함
- [x] hover와 같은 oversized `.risulua` CBS variable argument 경계에서 `{{getvar::ct_memory}}`의 `ct_memory` go to definition이 full fragment analysis empty guard 때문에 `.risuvar` default key나 workspace writer로 이동하지 못하던 문제를 보강함. `DefinitionProvider`에 oversized `.risulua` current-line fast path를 추가해 첫 번째 chat variable macro 인자만 `VariableFlowService`의 writer/default definition target으로 연결하고, macro 판별은 `getVariableMacroArgumentKind()` source-of-truth를 재사용하게 했으며 global/temp/value-slot/local-only 문맥은 추측하지 않게 유지함. Oracle review에서 지적한 target 없음 fallback 리스크도 fast path가 인자를 인식한 경우 즉시 `null`로 끝나도록 고정했고, oversized default `.risuvar` definition·workspace writer definition·missing target no-analysis·value-slot/no-chat 무추측 회귀 테스트, 수정 파일 LSP diagnostics, definition targeted test, hover+definition targeted test, cbs-lsp build 검증을 완료함
- [x] oversized `.risulua`에서 CBS builtin hover fast path가 `{{getvar::ct_memory}}`의 `getvar` macro name만 처리하고 `ct_memory` 같은 첫 번째 chat variable 인자는 full fragment analysis가 비어 hover가 사라지던 후속 회귀를 수정함. `HoverProvider`에 bounded current-line variable argument hover fast path를 추가해 `getvar`/`setvar` 등 chat variable macro 첫 인자만 보수적으로 tooltip을 만들고, global/temp/local-only 문맥은 full analysis 없이 추측하지 않게 유지함. Oracle review에서 지적된 1MiB 이후 뒤쪽 line 실패 리스크도 cursor 주변 line 추출 방식으로 고쳤으며 정상 `.risulua` string literal hover·oversized 뒤쪽 line hover·non-chat oversized 무추측 회귀 테스트, 수정 파일 LSP diagnostics, hover targeted test, cbs-lsp build 검증을 완료함
- [x] oversized `.risulua` CBS fast path 추가 전 CBS 인자 문맥을 core parser/tokenizer, completion context/provider, scope macro rules, diagnostics, hover/definition/signature, variable-flow, oversized guards 기준으로 재전수조사함. `metadata::` 첫 번째 인자는 current-line prefix만으로 안전하게 판별 가능하고 정적 catalog만 쓰는 문맥이라 `CompletionProvider`의 oversized macro-argument fast path에 추가해 `{{metadata::mo}}`에서 `mobile` 같은 metadata key 후보를 full fragment analysis 없이 반환하도록 보강함. 또한 `equal`/`greaterequal`/`less`/`and`/`contains` 같은 comparison/logical value 인자도 nested CBS 값을 받는다는 점을 반영해 workspace chat variable 후보를 bare name이 아니라 `{{getvar::name}}` value expression으로 삽입하게 추가함. `call::`/`arg::`/`slot::`/temp/global variable 인자처럼 local scope 또는 fragment symbol table이 필요한 문맥은 거대 `.risulua`에서 추측하지 않고 빈 completion으로 즉시 종료하도록 고정해 `locatePosition()` 우회를 유지함. Oracle review에서 지적된 current-line scan 상한 리스크도 1MiB cap과 cap 초과 시 빈 completion 종료 회귀 테스트로 닫았으며, 수정 파일 LSP diagnostics·oversized completion/fast-path boundary targeted test·`.risulua` CBS integration filtered test·large-workspace perf test·cbs-lsp build 검증을 완료함
- [x] oversized `.risulua` CBS fast path 작성 방식을 다음 작업자가 이어받을 수 있도록 handoff 문서 `docs/superpowers/plans/2026-04-25-oversized-risulua-fast-path-handoff.md`를 작성함. full analysis를 대체하지 않는 gate 원칙, bounded current-line scan만 허용하는 성능 계약, 안전한 문맥 판별 기준, lightweight 후보 소스, textEdit range 정책, stale workspace metadata 정책, 필수 unit/integration/perf/build 검증 루프, `split()` 회귀·operator-only `#when`·value argument 오탐 같은 자주 생기는 실수를 정리함
- [x] 512KiB 초과 `.risulua`의 oversized completion fast path에서 `{{#when::...}}` segment 인자도 operator와 workspace chat variable 후보를 함께 반환하도록 보강함. `{{#when::`와 `{{#when::mood::` 같은 현재 줄 prefix를 bounded scan으로 판별해 `is`/`and`/`or` 등 operator와 `mood`/`target`/`score` 같은 variable 후보를 동시에 제공하며, 일반 파일은 기존 fragment analysis 기반 `when-operators` 경로를 유지함. completion targeted test·`.risulua` CBS integration filtered test·large-workspace perf test·cbs-lsp build·수정 파일 LSP diagnostics 검증을 완료함
- [x] `packages/vscode/lua-sample/super-huge.risulua`처럼 512KiB를 넘는 `.risulua`에서 full CBS fragment analysis가 의도적으로 empty analysis로 축소된 뒤 `{{getvar::}}` 같은 CBS variable argument 위치의 Trigger Suggest가 `No suggestions`로 끝나던 경계를 보강함. `CompletionProvider`가 oversized `.risulua`의 현재 줄에서 안전한 `{{getvar::...`/`{{setvar::...` 계열 변수 인자 prefix만 bounded fast path로 판별해 workspace chat variable summary 후보를 반환하게 했고, 정식 fragment 분석은 계속 건너뛰어 oversized guard를 유지함. completion targeted test·`.risulua` CBS integration filtered test·large-workspace perf test·cbs-lsp build·수정 파일 LSP diagnostics 검증을 완료했으며, `lsp-server-integration.test.ts` 전체 실행의 workspace-state 관련 기존 실패 18건은 기존 `#### Backlog` remain task와 같은 범위로 재확인함
- [x] `.risulua`에서 `{{getvar::}}` 같은 CBS variable argument 영역과 `#when` operator 영역의 trigger suggestion이 퍼포먼스 개선 이후 비정상 동작할 수 있는 원인을 조사하고, graphify code graph·completion provider/context·server routing·기존 테스트 커버리지를 대조해 수정 계획 문서 `docs/superpowers/plans/2026-04-25-risulua-cbs-argument-suggestions.md`를 작성함. 주요 리스크를 cheap root fast path 경계, `.risulua` string literal fragment mapping, cached analysis/locatePosition 재사용, `addvar` coverage gap, workspace freshness gating으로 정리하고 테스트 우선 수정 순서를 확정함
- [x] `.risulua` CBS string literal 안의 `{{getvar::}}`/`{{addvar::}}` variable argument completion과 `{{#when::...::}}` operator+variable completion 회귀를 테스트 우선으로 보강함. `completion.test.ts`의 cheap root fast path boundary, `addvar` first/value argument, unclosed `{{getvar::mo`/`{{setvar::mood::`/`{{addvar::mood::` live typing recovery 회귀를 통과시켜 `::`/`?`/`:` argument/operator 문맥이 fragment analysis로 넘어가고 PlainText recovery가 실제 argument index를 유지하는 정책을 고정했으며, 빈 `{{calc::}}` 첫 인자는 `completion-context.ts`에서 close-tag가 아닌 calc-expression으로 분류하게 보강함. 실제 `.risulua`에서 `{{getvar::` 입력 직후 workspace snapshot이 stale이 되어 workspace 변수 후보가 숨겨지던 원인을 확인하고 macro-argument completion에서는 stale snapshot 후보도 보여주되 metadata로 stale 상태를 유지하게 `CompletionProvider`를 조정함. `lsp-server-integration.test.ts`의 `.risulua` string literal CBS argument/operator routing, stale snapshot workspace variable visibility, cached `getvar`/`#when` completion reuse 테스트와 `large-workspace.test.ts`의 oversized `.risulua` fragment-dependent completion guard도 확인함. 수정 파일 LSP diagnostics·completion targeted test·risulua-cbs integration targeted test·cached integration targeted test·large-workspace perf test·cbs-lsp build 검증을 완료함
- [x] Rust/WASM Lua lexical indexing kernel을 CBS LSP `.risulua` 분석 경로에 연결함. WASM analyzer가 Lua short/long string literal range, CBS marker 여부, 정적 `getState`/`setState`/`getChatVar`/`setChatVar` key occurrence를 compact record로 반환하고, TypeScript adapter/backend가 WASM lazy-load와 `luaparse` fallback을 관리하게 했으며, `.risulua` CBS fragment mapping을 full-file이 아닌 CBS-bearing string literal content range 기반으로 전환함. `FragmentAnalysisService.analyzeDocument()` 실제 provider 경로가 WASM literal range를 동기 lazy-load로 사용하고, oversized guard는 WASM/fallback/full-file mapping보다 먼저 실행되도록 유지함. wasm/core/cbs-lsp targeted tests와 build 검증을 완료함
- [x] 512KiB 초과 `.risulua`에서 completion은 복구됐지만 CBS 함수 hover와 Lua `getState`/`setState` 변수 flow가 비어 보이던 후속 회귀를 수정함. oversized `.risulua`는 여전히 full `mapToCbsFragments()`/`analyzeLuaSource()`를 타지 않게 유지하면서, `HoverProvider`가 현재 줄의 CBS builtin 이름만 bounded fast path로 tooltip을 만들게 했고, `FileScanner`가 원문을 `text: ''`로 유지하되 정적 `getState`/`setState`/`getChatVar`/`setChatVar` 문자열 key occurrence만 lightweight scan으로 보존해 `ElementRegistry`와 `UnifiedVariableGraph`에 `lua-state-api` read/write로 반영되게 함. 수정 파일 LSP diagnostics·element-registry/large-workspace regression tests·hover/completion feature tests·server-helper/LuaLS guard tests·cbs-lsp build 검증을 완료함
- [x] `packages/vscode/lua-sample/super-huge.risulua`처럼 512KiB를 넘는 `.risulua`에서 CBS LSP request가 통째로 사라져 completion이 빈 응답처럼 보이던 회귀를 수정함. `createFragmentRequest()`가 oversized `.risulua`도 `FragmentAnalysisRequest`로 유지하게 되돌리고, 기존 `FragmentAnalysisService` oversized guard가 parser/tokenizer 없는 empty analysis를 반환하게 해 full-file CBS 분석은 계속 차단하면서 `{{` cheap root completion은 즉시 응답하도록 정리함. `tests/perf/large-workspace.test.ts`에 실제 `super-huge.risulua` 샘플 기반 runtime budget 회귀 테스트를 추가해 request 유지, empty analysis, `user` completion, 250ms 예산을 고정했으며 수정 파일 LSP diagnostics·targeted perf test·completion/LuaLS guard tests·cbs-lsp build 검증을 완료함
- [x] 거대 `.risulua`를 한 번 열면 VS Code/CBS LSP가 먹통이 되는 문제의 즉시 방어선을 추가함. `packages/cbs-lsp`에 공용 oversized Lua guard를 추가해 opened-document `createFragmentRequest()`가 512KiB 초과 `.risulua`를 provider 분석 요청으로 만들지 않게 했고, `FragmentAnalysisService` 직접 호출 경로도 parser/tokenizer를 태우지 않는 empty analysis로 빠르게 반환하게 보강함. `createFragmentRequest()`가 `null`을 반환하는 routed `.risulua`도 LuaLS proxy skip으로 취급해 기존 oversized LuaLS timeout guard가 우회되지 않게 했으며, `packages/vscode` legacy CBS bracket pair highlighter와 autoSuggest fallback은 `.risulua`가 같은 threshold를 넘으면 `document.getText()`/suffix scan 없이 decoration·suggest trigger를 건너뛰도록 수정함. oversized `.risulua` fragment request/analysis/LuaLS skip/autoSuggest skip 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·targeted cbs-lsp tests·cbs-lsp build·VS Code build·VS Code boundary test·`npm run wasm:check` 검증을 완료함
- [x] `feat/architecture-rework` 기반 별도 worktree `../risuai-workbench-rust-wasm`에서 Rust WASM Lua analyzer 테스트환경을 구성함. `packages/lua-analyzer-wasm`에 `wasm-bindgen` 기반 `analyze_lua(source, options_json) -> string` 초기 API, compact JSON envelope 반환, Rust unit test, wasm-bindgen Node test, generated `pkg` 로드 smoke test, package README/LICENSE를 추가했고 루트에 `wasm:build`/`wasm:test:rust`/`wasm:test:wasm`/`wasm:smoke`/`wasm:check` 스크립트를 연결함. `npm run wasm:check`로 Rust unit 2건, wasm Node test 1건, Node smoke test 통과를 확인함
- [x] Rust WASM 기반 Lua analyzer 도입을 별도 worktree에서 실험하기 위한 계획 문서 `docs/rust-wasm-plan.md`를 생성함. 거대 `.risulua` 병목의 현재 방어 범위와 남은 runtime CBS provider 병목을 정리하고, Rust WASM의 목표/비목표, 신규 `packages/lua-analyzer-wasm` 후보 구조, WASM compact result API, `.risulua` string literal 기반 CBS fragment 전환, state API occurrence 추출, backend fallback 정책, 성능·패키징 검증 계획, worktree/PR 분할 전략을 단계별로 문서화함
- [x] 거대 `.risulua` 문제 후속 리서치를 위해 `docs/research/enomous-risulua-problem.md` 조사 노트를 생성함. 실제 문제 workspace(`chikan-train-latest`, `merry-sisters-final-1`)의 파일 수·크기·라인 수 메트릭, CBS LSP 병목 경로, 512KiB oversized guard 적용 내역, 관련 테스트와 검증 명령, VS Code/TypeScript/LuaLS/clangd/ESLint의 대형 파일 대응 선례, 후속 리서치 질문을 한 문서에 정리함
- [x] 거대 `.risulua`가 포함된 charx workspace에서 CBS LSP가 workspace indexing/LuaLS mirror sync와 request proxy timeout에 오래 묶이는 문제를 완화함. Layer 1 `FileScanner`가 512KiB 초과 Lua source를 index text에서 제외하되 원본 길이와 truncation flag를 scan record에 남기고, `ElementRegistry`는 해당 flag를 보고 `analyzeLuaSource()`를 건너뛰어 queryable file record만 유지하도록 수정함. LuaLS document router/process도 같은 기준으로 workspace bulk sync·standalone open document·shadow `.lua` write를 방어하고 `oversizedSkipped` 통계를 timeline trace에 포함하도록 보강했으며, deferred LuaLS sync timer가 stale oversized 문서를 나중에 다시 sync하지 않도록 취소 경로를 정리함. 또한 hover/completion request path에서도 현재 문서 text가 oversized면 LuaLS proxy 호출을 건너뛰어 CBS/local-only 결과로 즉시 끝나게 했고, Oracle review에서 지적된 timeout 잔존 리스크를 회귀 테스트로 닫음. 수정 파일 LSP diagnostics·server-helper/element-registry/luals-documents/luals-process/workspace-refresh targeted tests·cbs-lsp build 검증을 완료함
- [x] Extension Development Host에서 CBS completion이 Output에는 `start`만 반복되고 suggest widget은 loading에 머무는 상황을 추적하기 위해 CBS LSP durable timeline JSONL 로그를 추가함. VS Code client가 `context.logUri/cbs-lsp/timeline.jsonl` 경로를 `CBS_LSP_TIMELINE_LOG`로 embedded/standalone server에 전달하고, server tracing 공통 hook이 `trace`/`info`/`warn` event를 append-only JSONL로 남기도록 보강함. 또한 completion handler 예외 시에는 timeline/Output에 `error`와 `end`를 남기고 빈 completion 응답으로 복구해 request가 열린 채 남지 않도록 좁게 방어했으며, `.risuregex` `@@@ IN` 내부 bare `{{` fast path와 timeline 파일 기록 회귀 테스트를 추가함. 수정 파일 LSP diagnostics·신규 targeted test·cbs-lsp build·VS Code build 검증을 완료했고, 기존 넓은 `lsp-server-integration.test.ts` 전체 실행은 workspace-state 관련 기존 실패 18건을 별도 remain risk로 확인함
- [x] CBS completion 디버깅이 Output 없이 눈가리고 진행되던 문제를 줄이기 위해 `packages/vscode` client-side auto-suggest fallback에 운영 로그를 추가함. `{{`/`::` predicate hit, debounce schedule, duplicate skip, inactive editor/selection moved skip, auto-close short-circuit, 실제 `editor.action.triggerSuggest` 실행 시점을 모두 `CBS Language Server` Output channel에 남기고, 각 로그에 `clientStarted`/`clientRunning` 상태와 languageId·문서 version·cursor 위치를 함께 기록하도록 보강함. 수정 파일 LSP diagnostics·VS Code build·CBS client boundary test 12개 검증을 완료함
- [x] `packages/vscode` embedded CBS Language Server debug launch가 `--inspect=6009`를 하드코딩해 VS Code Extension Development Host를 여러 개 띄우면 inspector 포트 충돌이 나던 문제를 수정함. embedded debug `execArgv`를 `--inspect=0`으로 바꿔 Node가 사용 가능한 inspector 포트를 자동 선택하도록 했고, 수정 파일 LSP diagnostics·VS Code build·CBS client boundary test 12개 검증을 완료함
- [x] CBS LSP completion 지연 조사에서 Output에 completion 로그가 전혀 보이지 않는 원인을 구분할 수 있도록 `completion start` 운영 로그를 추가함. 이제 completion 요청이 서버에 도착하는 즉시 `[cbs-lsp:completion] start ... routedToLuaLs=...`가 출력되며, 이후 `build`/`luaProxy completion-end` duration 로그가 따라오므로 서버 미도달과 서버 내부 병목을 구분할 수 있음. 수정 파일 LSP diagnostics·completion/responseMerger/luals-proxy targeted test·cbs-lsp build·large-workspace perf test 검증을 완료함
- [x] CBS LSP 성능 개선이 실제 VS Code 런타임에서 체감되지 않는 원인을 후속 조사함. 현재 VS Code client는 workspace local `cbs-language-server` binary가 없으면 monorepo 개발용 `packages/cbs-lsp/dist/embedded.js`를 runtime path로 직접 로드하므로, `cbs-lsp` rebuild 뒤에도 이미 떠 있는 Extension Host/CBS server 프로세스는 자동으로 새 코드를 물지 않는다는 점을 확인함. 기존 `traceFeatureResult()`는 VS Code client trace 설정 없이는 Output에 보이지 않는 `connection.tracer.log()` 경로라, completion/Lua proxy duration을 `connection.console.log()` 운영 로그로도 내보내 Output > CBS Language Server에서 바로 확인할 수 있게 보강했으며 luals-proxy/completion/responseMerger targeted test·cbs-lsp build·large-workspace perf test 검증을 완료함
- [x] CBS LSP trigger suggest 체감 지연을 줄이기 위해 `VariableFlowService`에 cached lightweight completion summary를 추가하고, workspace chat variable completion 및 Lua `getState`/`setState` overlay completion이 후보마다 `queryVariable()`/default definition lookup을 반복하지 않도록 수정함. diagnostics fallback은 `.risulua`의 CBS101/CBS102 chat macro argument로 제한하고 publish-scope memoization·duration/count/code별 tracing을 추가했으며, 첫 `didOpen` full rebuild는 deferred job으로 분리해 local diagnostics가 먼저 publish되도록 바꿈. LuaLS workspace sync는 열린 `.risulua`를 우선 sync하고 나머지는 deferred batch로 넘기며 total/lua/skipped/synced/deferred/shadow duration tracing을 추가함. completion/responseMerger/diagnostics/workspace refresh/LuaLS targeted test·large-workspace perf test·cbs-lsp build 검증을 완료함
- [x] CBS LSP workspace indexing이 VS Code extension host/개발 repo처럼 큰 workspace에서 `node_modules`, `dist`, `graphify-out`, cache/build 산출물까지 재귀 순회해 startup과 auto suggest를 지연시키던 문제를 완화함. Layer 1 `FileScanner`의 async/sync scan 모두 dependency/build/cache 디렉토리를 건너뛰게 하고, `node_modules` 안의 `.risu*` 파일을 무시하는 회귀 테스트를 추가했으며 file-scanner/controller targeted test·lsp-server integration test·cbs-lsp build 검증을 완료함
- [x] `.risulua` hover 라우팅에서 CBS hover 대상(`{{user}}`, `{{getvar::...}}` 등)이 있어도 LuaLS hover proxy 응답을 기다리느라 VS Code에서 툴팁이 사라질 수 있던 문제를 수정함. `.risulua` 문서에서 CBS provider가 hover를 만든 경우에는 LuaLS 요청을 건너뛰고 즉시 CBS hover를 반환하도록 `server-helper.ts`를 보강했으며, LuaLS unavailable 상태에서도 builtin/function CBS hover가 LuaLS 요청 없이 반환되는 회귀 테스트를 갱신함. cbs-lsp integration targeted test·cbs-lsp build 검증을 완료함
- [x] `.risulua` 안의 CBS function 인자(`{{getvar::ct_NoStretchSpeech}}` 등)가 Layer 1 occurrence에 없더라도 이름 기반 workspace/default variable query로 보강되도록 수정함. `.risuvar` default-only 변수를 `VariableFlowService.queryVariable()`이 빈 occurrence node로 반환하게 하고, hover/diagnostics local-first 경로가 `resolveVariablePosition()` fallback으로 기본값을 찾아 `CBS variable "..." is referenced without a local definition` 오진을 억제하도록 보강했으며 `.risulua` definition/hover/diagnostics 회귀 테스트를 추가함. cbs-lsp integration targeted test·variable-flow service test·completion feature test·diagnostics-engine test·cbs-lsp build 검증을 완료함
- [x] CBS LSP `#each` shorthand/regular header의 정적 iterator source가 chat variable read처럼 definition/references/document highlight와 Layer 1 graph에 잡히도록 보강함. `extractEachLoopBinding`에 iterator range를 추가하고 `{{#each var1 key}}`에서 `var1`은 chat variable read, `key`/`slot::key`는 기존 loop alias로 유지되게 scope/reference/local-first/core occurrence 추출을 정리했으며 관련 feature/scope/indexer 회귀 테스트를 추가함
- [x] CBS LSP hover에서 `{{#each var1 key}}{{slot::key}}{{/each}}` 형태의 shorthand each 구문이 `var1`과 `slot` hover를 표시하지 않던 문제를 수정함. `#each` block header의 iterator를 chat variable hover로 해석하고, `#each A key` alias shorthand를 scope binding 추출에 반영했으며, pure-mode body 안에서도 허용된 `slot` macro 이름 자체는 builtin hover가 유지되도록 보강함. `{{#each items as}}` malformed 진단 회귀를 막기 위해 `as`를 shorthand alias로 인정하지 않게 좁혔고, hover 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·Prettier check·affected cbs-lsp tests·cbs-lsp 전체 테스트·cbs-lsp build 검증을 완료함
- [x] `packages/vscode` CBS block auto-close가 이미 `{{#if condition}} {{/if}}` 또는 `{{#if condition}}\n\n{{/if}}`로 닫힌 statement의 condition 내부에서 `{{...}}` CBS completion을 마무리할 때 `{{/if}}`를 중복 삽입하던 문제를 수정함. 커서 뒤 같은 줄의 남은 block header 닫힘 `}}`뿐 아니라 다음 줄의 기존 close tag까지 `documentSuffix`로 감지해 자동 close를 건너뛰도록 `cbsAutoSuggest`/`cbsAutoSuggestCore`를 보강하고, 같은 줄·멀티라인 close tag 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·Prettier check·VS Code build·client boundary test 12개 검증을 완료함
- [x] CBS LSP `#when`/문서용 block body가 공백만 포함할 때 CBS103 empty body 진단을 내던 문제를 수정함. 구조 진단의 `PlainText` body 판정을 trim 기반이 아니라 원문 길이 기반으로 좁혀 사용자가 의도적으로 남긴 공백을 body 존재로 취급하도록 바꾸고, `{{#when::ct_generatedHTML::vis::A}} {{/when}}` 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·diagnostics-engine targeted test·cbs-lsp build 검증을 완료함
- [x] CBS LSP `#when` header completion에서 `{{#when::arg1::arg2::arg3}}`의 각 segment 위치가 operator 후보만 반환하던 문제를 수정함. `when-operators` context가 operator 후보와 Risu chat variable 후보를 함께 반환하도록 `CompletionProvider`에 segment completion builder를 추가하고, `{{#when::`, `{{#when::mood::`, `{{#when::mood::is::` 위치 모두에서 operator와 workspace variable이 같이 뜨는 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·completion targeted test·cbs-lsp 전체 테스트·cbs-lsp build 검증을 완료함
- [x] CBS LSP `#when` header operator diagnostics가 중첩 CBS macro 내부의 `::`를 top-level segment 구분자로 오인해 `{{and::{{equal::...}}}}` 같은 유효한 complex condition에서 `Invalid #when operator "{{equal"` CBS003을 내던 문제를 수정함. block header segment splitter가 `{{`/`}}` depth를 추적해 최상위 `::`만 분리하도록 보강하고, nested `and`/`equal`/`getglobalvar` 조건 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·diagnostics-engine targeted test·cbs-lsp 전체 테스트·cbs-lsp build 검증을 완료함
- [x] `packages/vscode` CBS auto suggest fallback이 `{{#when::...}}` header argument 영역 전반에서도 `::` 입력 직후 suggestion을 다시 열도록 확장함. `{{#when::`, `{{#when::keep::var1::`, `{{#when::{{getvar::el_popup}}::`처럼 keep modifier·다중 segment·중첩 CBS macro 뒤에서도 열린 `#when` header depth를 추적해 trigger하고, 이미 닫힌 `{{#when::ready}}::` 뒤에서는 오작동하지 않도록 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·VS Code build·client boundary test 12개 검증을 완료함
- [x] CBS LSP builtin completion에서 CBS function/block syntax가 suggest 선택만으로 full macro snippet으로 자동완성되도록 개선하고, VS Code client-side filtering이 full macro `textEdit` 범위 때문에 후보를 숨기지 않도록 compact `filterText`를 추가함. 인자가 없는 `user`/`char` 계열은 `{{user}}`/`{{char}}`, `{{greaterequal}}` 계열은 `{{greaterequal::${1:a}::${2:b}}}`, `{{#when}}`은 `{{#when::${1:condition}}}`, `{{#each}}`는 `{{#each ${1:iterable} ${2:key}}}{{slot::${2:key}}}{{/each}}` 형태로 들어가며, unresolved payload에서는 `textEdit.newText`와 중복되는 `insertText`를 생략해 payload budget을 유지함. 수정 파일 LSP diagnostics·completion targeted test·large-workspace perf targeted test·cbs-lsp build·cbs-lsp 전체 테스트·VS Code build·수정 파일 Prettier 검증을 완료함
- [x] `packages/vscode`의 `.risulorebook`, `.risuregex`, `.risuprompt` TextMate grammar에 custom extension front matter highlighting을 추가함. 선두 `---` delimiter, `key: value` field, boolean/null/number/string/object-like value, `@@@ SECTION` marker를 별도 scope로 분리했으며 수정 syntax JSON 파일 LSP diagnostics·JSON parse smoke·VS Code build 검증을 완료함
- [x] `packages/vscode`의 `.risulua`, `.risuhtml`, `.risulorebook` TextMate grammar가 CBS 전용 highlighting 이후 각각 VS Code 내장 Lua(`source.lua`), HTML(`text.html.basic`), Markdown(`text.html.markdown`) grammar를 fallback으로 include하도록 보강함. 수정 syntax JSON 파일 diagnostics·JSON parse smoke·VS Code build 검증을 완료함
- [x] `.risulua` hover/completion 라우팅에서 LuaLS companion이 없거나 빈 응답을 돌려도 CBS provider 결과가 유지되도록 `server-helper.ts`의 Lua proxy 경로에 CBS fallback/merge를 추가함. `.risulua` 문서의 `{{user}}` hover와 `{{` root completion이 LuaLS unavailable 상태에서도 CBS builtin 후보를 반환하는 `lsp-server-integration.test.ts` 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·cbs-lsp integration targeted test·cbs-lsp build 검증을 완료함
- [x] CBS LSP completion에서 `{{#each A B}}{{/each}}`의 iterator 위치(`A`)가 `Block` header로만 분류되어 variable completion으로 이어지지 않던 문제를 수정함. `CompletionTriggerContext`가 `#each` open header의 첫 iterator 표현식 범위를 직접 계산해 chat-variable completion을 반환하도록 보강했고, prefix가 있는 `{{#each it}}`와 빈 iterator 위치 `{{#each }}` 회귀 테스트를 추가함. alias 생략 허용 정책 이후 stale해진 codeAction integration fixture도 `{{slot::item}}` 단독 오용 케이스로 갱신했으며 수정 파일 LSP diagnostics·completion targeted test·cbs-lsp 전체 테스트·cbs-lsp build·VS Code build 검증을 완료함
- [x] CBS `#each` block header에서 `as <item>` alias를 생략한 `{{#each items}}` 구문도 유효하게 취급하도록 parser 회귀 케이스와 LSP block-header 진단 정책을 보강함. core parser pure-mode 테스트에 alias 생략 케이스를 추가하고, cbs-lsp 진단은 `as` keyword를 실제로 시도했지만 alias가 깨진 경우만 `MissingRequiredArgument`를 내도록 좁혔으며, 기존 malformed fixture를 alias 생략 허용 fixture로 갱신함. 수정 파일 LSP diagnostics·core parser targeted test·cbs-lsp diagnostics/codeActions/references/rename targeted test·core build·cbs-lsp build 검증을 완료함
- [x] CBS LSP completion 반응성 3차 개선으로 `WorkspaceRefreshController`의 `document-change` workspace rebuild/diagnostics 경로를 150ms debounce하도록 변경해 새 입력 직후 completion 요청이 동기 incremental rebuild 뒤에 줄 서던 병목을 줄임. open/close/watched-file/configuration refresh는 기존처럼 즉시 처리하되, close/watched/configuration 경계에서는 pending document-change refresh를 먼저 flush해 상태 손실을 방지함. `flushDocumentChangeRefresh()` 테스트 seam과 `tests/controllers/workspace-refresh-controller.test.ts`를 추가해 change 직후 diagnostics publish가 즉시 실행되지 않고 debounce 이후 실행되는 정책과 close 전 flush 정책을 고정했으며, 기존 integration test도 debounce 이후 diagnostics를 확인하도록 조정함. 수정 파일 LSP diagnostics·controller/integration targeted test·cbs-lsp build·cbs-lsp 전체 테스트·VS Code build 검증을 완료함
- [x] CBS LSP completion 2차 개선으로 `{{` / `{{foo` / `{{#` / `{{#w` root prefix 전용 fast path를 `CompletionProvider`에 추가해 해당 문맥에서는 `analysisService.locatePosition()`과 fragment diagnostics 분석을 거치지 않고 builtin/block/snippet 후보를 즉시 반환하도록 최적화함. current-line cheap detector는 sectioned artifact에서 CBS-bearing section만 허용하고 pure/puredisplay body, `{{getvar::` 같은 argument completion, `{{?` calc, `{{/` close tag, `{{:` else 문맥은 기존 full analysis로 fallback하도록 보수적으로 제한했으며, fast path textEdit이 `{{us`→`{{user`, `{{#w`→`{{#when`처럼 prefix를 중복하지 않도록 보정함. completion fast path hit/fallback 회귀 테스트를 추가했고 수정 파일 LSP diagnostics·completion/payload targeted test·cbs-lsp build·cbs-lsp 전체 테스트·VS Code build 검증을 완료함
- [x] CBS LSP `CompletionProvider.resolve()`가 단일 completion item hydrate를 위해 `provide()`/`provideInternal()` 전체 분석 경로를 다시 실행하지 않도록 수정함. unresolved payload 크기 회귀를 피하기 위해 별도 `resolveKey` 직렬화 없이 기존 category/label/sortText에서 hydrate 경로를 파생하고, builtin은 registry lookup으로, workspace chat variable은 `VariableFlowService.queryVariable()`과 default variable definition 조회로, local variable은 category 기반 lightweight metadata로 detail/documentation/explanation/workspace metadata를 직접 복원하며, hydrate 불가능한 항목은 기존처럼 `null`을 반환하도록 정리함
- [x] cbs-lsp completion handler와 completion resolve handler에 `performance.now()` 기반 duration tracing을 추가함. `server-helper.ts`의 `registerCompletionHandler` CBS path와 `registerCompletionResolveHandler`에서 completion list/resolve 결과 생성 시간을 측정해 `traceFeatureResult`로 `durationMs`를 기록하고, `RequestHandlerRunner`의 `summarize` end trace에도 `durationMs`를 포함함. `WorkspaceRefreshController.ts`의 `refreshTrackedWorkspaces`와 `refreshWorkspaceUris`에서 `state-rebuild-start`가 실제 rebuild 호출 **이후**에 기록되던 버그를 rebuild **이전**으로 이동시키고, `state-rebuild-end`에 `durationMs`를 추가함. 수정 파일 LSP diagnostics·cbs-lsp build·cbs-lsp 전체 테스트 검증을 완료함
- [x] `packages/vscode/src/completion/cbsAutoSuggest.ts`의 중복 `editor.action.triggerSuggest` 호출을 debounced `scheduleCbsSuggest`로 교체하고, 동일 document version + position 기준 재요청 guard를 추가해 `{{` 입력 시 3~5회 동시 completion request를 유발하던 request storm을 제거함. auto-close 로직과 predicate는 그대로 유지했고, extension dispose 시 pending timer를 정리하는 wrappedDisposable을 추가했으며 수정 파일 LSP diagnostics·VS Code build 검증을 완료함
- [x] CBS 개발자 확인에 따라 `{{#if_pure}}`가 공식적으로 `{{/if}}`로도 닫히는 문법을 core parser에 반영함. `if_pure` close alias로 `/if`를 허용해 기존 `{{/if_pure}}`·`{{/}}` 경로와 함께 진단 없이 닫히도록 수정하고, `packages/core/tests/domain/cbs/parser.test.ts`에 회귀 테스트를 추가했으며 수정 파일 LSP diagnostics·core parser targeted test·core build·cbs-lsp build 검증을 완료함. 기존 조사 결과도 `{{/if}}` 공식 close alias를 누락한 것으로 정정함
- [x] `packages/vscode` CBS auto suggest fallback에 block opener auto-close를 추가해 `{{#if}}`/`{{#each ...}}`/중첩 CBS 조건이 들어간 `{{#if {{getvar::enabled}}}}` 입력 직후 `{{/if}}` 같은 close tag가 자동 삽입되도록 했고, 자동 삽입된 close tag 변경 이벤트가 다시 auto-close를 반복하던 재귀 버그를 close-tag suffix guard로 차단했으며 수정 파일 LSP diagnostics·VS Code build·client boundary test 12개·수정 파일 Prettier 검증을 완료함
- [x] CBS LSP block snippet completion에서 `{{#each}}`처럼 auto-close된 block header 위에 `each-block`을 적용할 때 `{{##each...` / trailing `}}`가 중복되던 textEdit range 버그를 수정함. block snippet은 여는 `{{`부터 cursor 직후 `}}`까지 header 전체를 교체하고, 일반 block function completion은 기존 prefix replacement 동작을 유지하도록 분리했으며 completion 회귀 테스트·수정 파일 LSP diagnostics·cbs-lsp build 검증을 완료함

### Done (2026-04-24)

- [x] `packages/vscode` CBS 자동 suggest fallback이 active selection 대신 실제 `TextDocumentContentChangeEvent` 변경 끝점 기준으로 line prefix를 계산하도록 보강해 `{{` 입력 직후 `editor.action.triggerSuggest`가 입력 위치 기준으로 안정적으로 열리게 했고, 수정 파일 LSP diagnostics·VS Code build·client boundary test 11개·수정 파일 prettier 검증을 완료함
- [x] CBS tokenizer/parser가 `{{#if {{? {{getvar::ct_Language}} == 1}}}}` / `{{#if {{? {{getvar::ct_Deck_Level}} <= 2}}}}`처럼 block header 안 inline math expression에 중첩된 CBS macro token을 보존하도록 수정하고, `MathExpr` child AST를 visitor·fragment locator·scope collector·calc diagnostics가 순회/직렬화하게 해 inner `getvar` collector/autocomplete와 inline math diagnostics 오진을 함께 복구했으며 core tokenizer/parser/extract 회귀, cbs-lsp diagnostics/fragment-locator/scope/completion/integration 전체 테스트, core/cbs-lsp/VS Code build 검증을 완료함
- [x] VS Code CBS completion이 `{{`와 `::` 입력 직후 suggest widget을 더 안정적으로 열도록 client-side fallback을 보강하고, CBS LSP가 닫히지 않은 `{{getvar::` / `{{gettempvar::` / `{{metadata::` / `{{call::` 상태에서도 PlainText recovery context로 후보를 반환하도록 수정했으며, 변수명이 쓰이는 CBS macro 인수 위치를 scope analyzer 규칙 기준으로 통합해 `setdefaultvar`/global variable 인수에서도 suggestion·definition 규칙이 일관되게 동작하게 했고, `getvar` 계열 ctrl+click이 `.risuvar` 기본 변수 key로 다시 이동할 수 있도록 definition target을 복구하고 completion/definition/server seam/VS Code client boundary test를 검증함
- [x] `risu-cbs-syntax-highlighter`의 TextMate grammar를 Risu 언어별 grammar로 가져오고, CBS function database/completion/signature/parser/formatter core 및 double-brace bracket highlighter provider를 `packages/vscode/src/cbs/legacy`에 통합했으며, LSP와 중복되는 completion/hover/signature provider는 등록하지 않고 bracket decorator만 활성화하는 방식으로 VS Code build·boundary test·CBS LSP 회귀 검증을 완료함
- [x] Risu CBS 언어에서 `{{` 입력 직후 수동 `Trigger Suggest` 없이 자동 suggestion이 뜨도록 VS Code `configurationDefaults`에 `editor.suggestOnTriggerCharacters`와 문자열/일반 `quickSuggestions` 기본값을 추가하고, `{{` 입력 감지 시 `editor.action.triggerSuggest`를 호출하는 client-side fallback 및 `{{`/`}}` double-brace bracket language configuration을 보강했으며 predicate/manifest 테스트·VS Code build/e2e 검증을 완료함
- [x] CBS LSP initial completion latency를 줄이기 위해 `provideUnresolved()`가 `{{`/`{{#` 후보 생성 시 builtin detail/documentation/explanation을 먼저 만들고 버리던 경로를 lightweight builder로 분리하고, formatter 미호출 회귀 테스트·completion/integration/VS Code boundary 검증을 완료함
- [x] CBS LSP autocomplete 반응성을 높이기 위해 `ElementRegistry.rebuild()`의 파일별 eager snapshot rebuild를 배치 1회 rebuild로 줄이고, `UnifiedVariableGraph`의 URI occurrence/position 조회가 기존 cache를 바로 쓰도록 바꿨으며, workspace chat-variable completion의 중복 `queryVariable()` 호출을 제거하고 `{{}}` cursor-middle completion 회귀 및 feature-matrix on-type formatting test seam을 보강한 뒤 cbs-lsp 전체 테스트·빌드를 완료함
- [x] VS Code CBS completion이 `{{`/`{{#`처럼 아직 닫히지 않은 macro prefix에서도 후보를 내도록 plain-text recovery context 감지를 보강하고, untitled/language-mode 문서에도 LSP client가 붙도록 `packages/vscode` document selector에 language 기반 selector를 추가했으며 completion/boundary 회귀 테스트와 빌드 검증을 함께 완료함
- [x] cbs-lsp LuaLS sidecar vertical slice를 재판정해 real-binary diagnostics/hover/completion roundtrip이 현재 코드 기준 충족됐음을 checklist에 반영하고, standalone stdio product matrix에 generated RisuAI stub completion smoke를 추가했으며 README/LuaLS companion 문서와 TODO evidence를 함께 갱신함
- [x] core CBS builtin registry에 `contextual` 메타를 추가해 `slot`/`position`을 source-of-truth 기준으로 분류하고, `isContextualBuiltin`/`getContextual`/`isContextual` helper를 추가함. cbs-lsp completion/hover/diagnostics가 `contextual-builtin` kind를 같은 기준으로 읽도록 정리하고, 관련 회귀 테스트·checklist Evidence·TODO를 함께 갱신함
- [x] cbs-lsp client capability degraded matrix에 Neovim / Zed / Emacs / VS Code-family 최소 profile을 추가하고, fixture snapshot·capability/availability 회귀 테스트·COMPATIBILITY.md graceful degradation 문구·STANDALONE_USAGE.md client profile 섹션·CBS checklist Evidence·TODO를 함께 갱신함
- [x] `packages/vscode` CBS client boundary에 pure snapshot seam과 product-level `verify:cbs-client` 회귀 루프를 추가해 standalone local-devDependency / embedded fallback / invalid path override failure UX / multi-root reduced initialize preview를 built output 기준으로 고정했고, `packages/vscode/README.md`, `packages/cbs-lsp/docs/COMPATIBILITY.md`, `packages/cbs-lsp/docs/STANDALONE_USAGE.md`, `packages/cbs-lsp/checklist/CBS_CHECKLIST.md`를 같은 wording으로 갱신함
- [x] `packages/vscode` extension-host runtime E2E를 standalone explicit path override 전용에서 scenario matrix로 확장해 auto embedded fallback까지 실제 `LanguageClient` initialize → didOpen → hover → shutdown cleanup 경로를 고정했고, fixture workspace 기본 설정·VS Code client 문서·CBS checklist Evidence를 함께 갱신함
- [x] `packages/cbs-lsp/src/embedded.ts` IPC bootstrap entry를 추가하고 `packages/vscode` embedded fallback source-of-truth를 `dist/embedded.js`로 바로잡아, boundary snapshot에만 있던 monorepo fallback이 실제 extension-host runtime에서도 부팅되도록 복구함
- [x] `packages/vscode`에 extension-host runtime client E2E를 추가해 explicit standalone path override 기준 real `LanguageClient` initialize → didOpen → hover → shutdown cleanup 경로를 `@vscode/test-electron`으로 고정하고, boundary snapshot/failure-UX suite와 분리된 `test:e2e:cbs-client:runtime`·README·CBS checklist Evidence를 함께 갱신함
- [x] standalone server 검증을 `packages/cbs-lsp`의 `test:e2e:standalone` + `test:perf:standalone`로 분리하고, official VS Code client 검증을 `packages/vscode/tests/e2e/extension-client.test.ts` + `test:e2e:cbs-client`로 승격해 `verify:cbs-client`/README/CBS checklist wording이 같은 검증 레이어를 가리키도록 정리함
- [x] `packages/vscode/src/lsp/cbsLanguageClient.ts`에 minimal runtime-test seam을 추가해 `getCbsLanguageClientRuntimeState()`/`awaitCbsLanguageClientReady()`로 extension-host 테스트가 client readiness·boundary snapshot·cleanup 상태를 관찰하고 검증할 수 있도록 함
- [x] cbs-lsp real extracted workspace E2E를 standalone stdio interface 기준으로 먼저 구현하고, VS Code client E2E를 별도 client-layer concern으로 분리. `packages/cbs-lsp/tests/e2e/extracted-workspace.test.ts`에 stdio LSP E2E를 추가해 initialize→didOpen→diagnostics→cross-file references→completion→hover→workspace symbol→custom availability→shutdown 경로를 real extracted workspace로 검증하고, `packages/cbs-lsp/tests/product/stdio-helpers.ts`에 shared `StdioLspClient`를 추출해 `stdio-server.test.ts`와 재사용하게 함. `packages/vscode/tests/e2e/extension-client.test.ts`에 client-layer separation test를 추가해 서버/클라이언트 검증 경계가 명확히 분리됨을 고정하고, `packages/cbs-lsp/checklist/CBS_CHECKLIST.md` 5.6.2 항목을 완료 처리함
- [x] cbs-lsp checklist 5.6.2 large workspace benchmark를 완료 처리하고, `packages/cbs-lsp/tests/perf/large-workspace.test.ts`에 standalone server cold-start / incremental rebuild / official client attach cost를 분리 측정하는 stdio 기준 회귀 테스트를 추가함. `packages/cbs-lsp/checklist/CBS_CHECKLIST.md` 5.6.2 항목을 `[done]`로 전환하고 Evidence·touchpoint를 갱신함
- [x] cbs-lsp checklist 5.6.2 official client attach coverage gap을 닫음. `packages/cbs-lsp/tests/perf/large-workspace.test.ts`의 `official client attach cost` 블록 이름을 `stdio client attach cost`로 바로잡아 stdio 시뮬레이션과 공식 VS Code client 경계를 명확히 분리하고, 실제 official client attach 검증은 `packages/vscode/tests/e2e/extension-host/suite.ts`의 `@vscode/test-electron` 기반 real Extension Development Host LanguageClient 왕복에서 `performance.now()`로 attach/readiness 비용을 측정·검증하도록 추가함. `CBS_CHECKLIST.md` Evidence를 실제 official client runtime E2E를 명확히 인용하도록 갱신함
- [x] cbs-lsp checklist 5.5의 stale `[future]` editor integration E2E 항목을 현재 코드 기준으로 재판정해 완료 처리하고, standalone server 검증(`test:e2e:standalone`, `test:perf:standalone`)과 official VS Code client 검증(`test:e2e:cbs-client:boundary`, `test:e2e:cbs-client:runtime`)의 책임 경계를 `CBS_CHECKLIST.md`, `packages/cbs-lsp/README.md`, `packages/vscode/README.md`에 같은 wording으로 다시 고정함
- [x] cbs-lsp `textDocument/onTypeFormatting` vertical slice를 현재 코드 기준으로 재판정해 신규 구현하고, newline-only capability/handler/provider를 단일 clean CBS fragment 내부로 제한했으며 multi-fragment/non-CBS/malformed/recovery safe no-op 정책을 feature·integration test와 README/COMPATIBILITY/checklist Evidence에 함께 고정함
- [x] `packages/cbs-lsp/src/core/local-functions.ts`, `recovery-contract.ts`, `fragment-position.ts`의 exported 타입·인터페이스·함수에 AGENTS.md 규약 JSDoc을 보완하고, `fragment-position.ts`에는 `@file` 개요 주석을 추가했으며 수정 파일 LSP diagnostics와 `npm run --workspace cbs-language-server build` 검증을 완료함
- [x] `packages/cbs-lsp/src/core/completion-context.ts`, `calc-expression.ts`, `fragment-analysis-service.ts`의 파일 개요, exported 타입·인터페이스·함수·클래스 JSDoc을 AGENTS.md 규약에 맞춰 보강하고, 복잡한 completion/context/fragment analysis 분기에는 짧은 의도 주석을 추가함
