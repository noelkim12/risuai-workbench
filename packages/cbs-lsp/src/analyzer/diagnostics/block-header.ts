/**
 * diagnostics에서 재사용하는 block header 공용 barrel.
 * @file packages/cbs-lsp/src/analyzer/diagnostics/block-header.ts
 */

/** diagnostics collector가 기존 analyzer block-header contract를 같은 경로에서 재수출함. */
export * from '../block-header';
