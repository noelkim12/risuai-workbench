export const RECIPE = [
  '---',
  'source: generated',
  'page-class: manual',
  '---',
  '',
  '# Recipe: add new entry',
  '',
  'Trigger: user asks to create a new lorebook entry, NPC, or event.',
  '',
  '## Goal',
  'Create a new `.risulorebook` file in the extract directory with proper keywords, content, and CBS variable hookups.',
  '',
  '## Allowed edits',
  '- ✅ New `.risulorebook` file in the source extract `lorebooks/` directory',
  '- ✅ `notes/lorebook/<new-slug>.md` for design intent',
  '',
  '## Forbidden edits',
  '- ❌ `_generated/lorebook/` — this is regenerated by analyzer',
  '- ❌ `_folders.json` unless the user explicitly asked to place the entry in a specific folder',
  '',
  '## Steps',
  '1. Read `overview.md` and `lorebook/_index.md` to understand the existing entry catalog.',
  '2. Read 2-3 similar existing entries to match the authoring style (mode, keywords, CBS usage).',
  '3. Read `variables.md` to see what CBS variables are already in use — reuse existing names when the new entry interacts with existing state.',
  '4. Create the new `.risulorebook` file with frontmatter (keywords, activation mode) and content body.',
  '5. Tell the user to re-run analyze to regenerate the wiki and confirm the new entry and its Relations appear correctly.',
  '',
  '## Hard rules',
  '- ❌ Do NOT invent new CBS variable names when existing ones cover the need. Check `variables.md` first.',
  '- ✅ Use the same keyword casing and quoting conventions as neighboring entries.',
  '- ✅ If the new entry should activate an existing entry, mention the existing entry name in the content so the activation chain is picked up.',
  '',
].join('\n');
