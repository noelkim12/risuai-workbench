export const RECIPE = [
  '---',
  'source: generated',
  'page-class: manual',
  '---',
  '',
  '# Recipe: edit entity',
  '',
  'Trigger: user asks to modify a lorebook entry, NPC description, keywords, or regex script content.',
  '',
  '## Goal',
  'Produce a targeted edit to the source artifact file that the user will then re-extract.',
  '',
  '## Allowed edits',
  '- ✅ The `.risulorebook`, `.risuregex`, or `.risulua` file in the extract directory',
  '- ✅ `notes/lorebook/<slug>.md` to record design intent about this edit',
  '',
  '## Forbidden edits',
  '- ❌ Anything under `_generated/` — changes are wiped on next analyze',
  '- ❌ Anything under `_schema/` — regenerated by the wiki generator',
  '',
  '## Steps',
  '1. Read `overview.md` to confirm the artifact has the entity you want to edit.',
  '2. Read the entity page `lorebook/<slug>.md` for its current Relations and frontmatter.',
  '3. If the edit might affect neighbors (keywords, activation), read at most 2 neighbor entity pages from Relations.',
  '4. Open the source `.risulorebook` file referenced in the `Source:` line and apply the edit.',
  '5. Tell the user to re-run `risu-core analyze <target> --wiki` to regenerate the wiki.',
  '',
  '## Hard rules',
  '- ❌ Do NOT walk more than 2 hops in Relations. Use a chain file instead if broader context is needed.',
  '- ❌ Do NOT edit the entity page in `_generated/`; the source of truth is the extract file.',
  '- ✅ Record narrative in `notes/lorebook/<slug>.md` if the edit has non-obvious intent.',
  '',
].join('\n');
