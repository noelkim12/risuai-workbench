/**
 * Lorebook format state 타입을 format-local 경로에서도 발견할 수 있도록 재노출합니다.
 * @file packages/core/src/domain/editor/formats/lorebook/types.ts
 */

export type { LorebookEditorState } from '../../document-model/types';
