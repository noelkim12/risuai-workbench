/**
 * HTML format state 타입을 format-local 경로에서도 발견할 수 있도록 재노출합니다.
 * @file packages/core/src/domain/editor/formats/html/types.ts
 */

export type { HtmlEditorState } from '../../document-model/types';
