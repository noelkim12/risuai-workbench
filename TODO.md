# TODO

완료 이력과 운영 메모는 `FIN.md`에 기록합니다. 더 오래된 상세 아카이브는 `../docs/todo-done-archive.md`를 참고하면 됩니다.
현재 남은 작업은 아래 `### Remaining` 섹션만 기준으로 관리하며, 완료 이력은 `FIN.md`에만 추가합니다.
2026-05-08 CBS preview variable injector engine final bookkeeping 후 새 remain task는 없으며, 기존 `#### Backlog` 항목만 유지합니다.
최근 갱신: 2026-05-09 RisuLua modular extract에서 `--risulua-split module-table` 실패 시 전체 추출을 중단하지 않고 생성된 `lua/main.risulua`를 보존한 뒤 `.risuchar`/`.risumodule` 등 후속 canonical 산출물을 계속 쓰도록 fallback을 추가했으며, focused character/module tests(`charx-extract module-extract`, 2 files / 38 tests)와 packages/core build를 통과했습니다. Live no-manifest packed `.charx` extract QA도 `docs/bundle-mode/extract-test/round-trip-without-manifest/03-extract-packed-1-fallback/`에서 fallback warning과 함께 `lua/main.risulua` 보존 및 `.risuchar`/canonical 출력 생성을 확인했습니다. RisuLua modular pack/extract에 optional `--risulua-recovery full-source`를 추가해 packed `.charx`/`.risum` triggerlua dist 안의 compressed recovery manifest로 `lua/`, `docs/`, `legacy/` 파일을 복원하는 round-trip 경로를 구현했습니다. 기본 pack은 recovery metadata를 넣지 않아 기존 dist 크기와 동작을 유지하며, focused recovery/pack/extract/build tests와 packages/core build를 통과했습니다. RisuLua module-table host ABI shell hardening과 generated navigation metadata 구현을 완료했습니다. export manifest, axLLM/member-field 분석 보정, duplicate public global source-order bridge, listenEdit callback wrapper extraction, Merry RPG / Violated Girl fixture 재추출 범위를 확인했습니다. generated navigation comments / button action index를 추가했고, button action bridge `---@source`는 main 선언 위치가 아니라 non-main `risu-trigger`/CBS button 사용처가 있을 때만 해당 외부 source range를 가리키도록 보정했습니다. public domain function과 host/global helper function은 실제 호출 모듈에서 직접 module을 import하도록 정리했고, 최종 main.risulua 조립 후 사용되지 않는 `common.local_helpers` / `handler_helpers.*` require 라인도 alias 사용 여부 기준으로 제거하도록 보강했습니다. runtime handler host/global helper import, prompt/instruction store 참조, 추출 함수 local shadowing 보존 회귀 테스트를 추가했으며 targeted module-table tests 및 packages/core build를 통과했습니다. 외부 archive fixture가 없는 환경에서는 archive report-only QA suite를 skip하도록 보강했습니다. VS Code RisuLua/Lua TextMate grammar에 generated button action bridge 및 `---@source` navigation comment colorization scope를 추가했고, 관련 grammar boundary test와 VS Code package build를 통과했습니다. RisuLua modular bundler는 최종 dist 직렬화 시 parser comment range 기반으로 소스/번들러 내부 주석을 제거하되 문자열과 long bracket 문자열의 `--` 텍스트는 보존하도록 보강했고, generated dist header에는 재모듈화 입력으로 dist를 쓰지 말고 `lua/main.risulua` 또는 `legacy/original.risulua`를 쓰라는 안내를 추가했으며, targeted modular/dist header tests 및 packages/core build를 통과했습니다.

RisuLua `---@source` Go to Definition / hover fast-path 보정도 완료했습니다. `docs/superpowers/plans/2026-05-08-risulua-source-comment-definition.md` 계획에 따라 parser/resolver, LuaLS fallback wiring, server regression으로 generated `.risulua` source comment Go to Definition을 구현했습니다. source-comment definition/hover 처리를 CBS provider와 LuaLS fallback 작업보다 먼저 동기 반환하도록 `HoverRegistrar` / `DefinitionRegistrar` 앞단까지 끌어올려 이전 LuaLS hover/definition 요청이 pending이어도 다음 `---@source` hover/highlight가 async fallback queue에 묶이지 않도록 보강했습니다. VS Code client 쪽은 native LuaLS 기능을 보존하기 위해 기본 `*.risulua` association을 `lua`로 유지하고, `risulua` language contribution은 확장자 소유 없이 유지합니다. native VS Code Ctrl-click/F12 definition aggregation은 Lua provider까지 기다리는 구조라 완전한 즉시화가 어렵기 때문에, `---@source` path 부분에 `DocumentLinkProvider` 기반 command link를 추가해 즉시 클릭 가능한 대안 UX를 제공합니다. source-link command constant는 `cbsCommands.ts`로 분리해 무거운 LanguageClient import 없이 재사용하고, boundary test에서 `languageId=lua` `.risulua` 문서의 command URI 인자 직렬화를 검증했습니다. targeted source navigation/server-helper tests, cbs-language-server build, VS Code extension build, focused association/source-link boundary tests를 통과했습니다.

RisuLua generated bridge RHS Go to Definition 후속 보정도 완료했습니다. `M.removeActionButton = removeActionButton` 형태의 generated export alias를 Rust/WASM module member definition으로 수집하고, `__button_actions.removeActionButton` RHS member에서 definition을 요청하면 `lua/button_actions/actions.risulua`의 `M.removeActionButton` export로 이동하도록 보정했습니다. module target을 못 찾는 경우에는 LHS self-link로 오동작하지 않도록 bridge fast-path가 `null`을 반환해 fallback 경로에 맡기며, 관련 Rust/cbs-lsp targeted tests와 builds를 통과했습니다.

최근 갱신(이번 작업): 2026-05-09 RisuLua modular bundler를 단일 `__risulua_loaders` registry table 기반으로 전환하고 local-budget analyzer/dist diagnostics를 추가했습니다. 150/180/190 locals는 warning, 200 locals 이상은 validation-blocking error로 처리하며 focused tests/build를 통과했습니다.

분석/수정 메모: 2026-05-09 `docs/bundle-mode/extract-test/targets/`의 비-workbench 산 `.charx`/`.risum`은 `#risulua-bundle-manifest-v1` 복원 manifest가 없어 extract 시 단일 `lua/main.risulua` 보존 경로에서 시작하며, `--risulua-split module-table`은 이후 best-effort 재분해를 시도합니다. split workspace 자체가 실패하면 modular extract는 single-file fallback을 유지합니다. local budget analyzer는 wasmoon/Lua 정책대로 chunk/function scope별로 집계하고 있었으나, split workspace validator가 dist local-budget warning을 모두 error finding으로 매핑하던 문제를 수정해 150/180/190 locals는 non-blocking warning, 200 locals 이상만 blocking error로 유지했습니다. Merry RPG는 167 locals warning 상태로 module-table dist 생성/validation을 통과하고, Chikan Train(212/449 locals)과 Violated Girl(387 locals)처럼 generated dist만 200 hard limit을 초과하는 경우는 workspace split을 보존하되 plan/report에 non-packable dist validation failure로 기록합니다. table-backed loader registry는 top-level loader local 폭증만 해결하며, `runtime.button_click`/`runtime.output`/대형 domain module처럼 모듈 본문 자체가 큰 경우의 per-function local budget 문제는 별도 후속 설계가 필요합니다.

Round-trip QA 메모: 2026-05-09 `docs/bundle-mode/extract-test/improved_bundling/`에서 targets 기반 extract→pack→extract 검증을 active-local analyzer 보정 후 재수행하고 `roundtrip-local-budget-report.md` 및 aggregate JSON에 dist local 집계 결과를 갱신했습니다. `charx-chikan-train`, `module-merry-rpg-v1-3`, `module-violated-girl-260501` 모두 first extract, full-source recovery pack, second extract, recovered workspace repack이 통과했습니다. improved-bundling first/second extract dist는 local-budget warning/error 없이 통과하며, Chikan Train max active locals는 120, Merry RPG는 106, Violated Girl은 144로 집계됩니다. 기존 `output/` 비교군은 active-local 기준으로 Chikan Train과 Merry RPG가 통과하고, 구형 `output/module-violated-girl-260501` dist만 chunk max active locals 216으로 여전히 local-budget error 상태입니다. 이후 보고서는 중복되는 second-extract 세부 행을 제거하고 기존 `output/` dist와 개선 `first-extract` dist를 케이스별 1:1 비교 구조로 재정리했으며, `legacy/original.risulua` 3개도 local-budget 분석 도구로 집계해 원본 Lua → 기존 output → 개선 first-extract 3-way 비교 구조로 확장했습니다. Merry RPG 106 locals 지표는 880행 callback 자체가 아니라 15~915행 익명 IIFE 스코프가 880행에서 peak에 도달한 값임을 명확히 하도록 보고서 위치 표기를 scope range / peak line 기준으로 보강했습니다. 후속 구현 계획은 `docs/superpowers/plans/2026-05-09-risulua-module-local-budget-hardening.md`에 저장했으며, scope/peak diagnostics 보강과 safe export alias rewrite를 1차 hardening 범위로 정의했습니다.

Chikan 원인 보정: 과거 Chikan Train 성공 기록은 module-table dry-run/report 생성, host ABI shell 이동(`generateStartInput`, `skill_*`) 및 필수 파일 존재를 검증한 것이며, 현재 추가된 dist local-budget hard-limit 검증과는 기준이 다릅니다. 직접 artifact 단위 집계 결과 Chikan의 hard-limit은 `button_actions`가 아니라 `lua/runtime/output.risulua`의 `onOutput` 함수 scope 449 locals와 `lua/runtime/button_click.risulua`의 `onButtonClick` 함수 scope 212 locals에서 발생합니다. 다만 실제 `docs/bundle-mode/extract-test/improved_bundling/charx-chikan-train/first-extract/lua/`에는 `main.risulua`만 남아 있으므로 module-table workspace 파일 생성이 성공한 상태가 아니라, split report/plan만 남고 local-budget hard-limit 때문에 single-file fallback이 적용된 상태입니다. 후속 수정은 runtime handler body를 단일 함수로 보존하는 module-table 정책을 쪼개 pack/dist 검증을 통과시키는 방향이어야 합니다.

Split 우선 성공 보정: 2026-05-09 module-table extract에서 workspace 생성은 성공했지만 generated dist local-budget hard-limit만 실패하는 경우를 single-file fallback으로 취급하지 않도록 분리했습니다. `buildRisuLuaSplitDist()`는 dist 검증 blocking diagnostics를 throw하지 않고 build result에 담아 반환하며 실패 dist 파일은 제거합니다. `validateRisuLuaSplitWorkspace()`는 해당 diagnostics를 plan/report finding으로 흡수하고, `runModuleTableDryRunAsync()`는 dist-only blocking finding에서는 workspace commit을 유지합니다. 이후 local-budget analyzer를 Lua compiler와 같은 active-local semantics로 보정했습니다. 기존 analyzer는 function/chunk별 `local` 선언을 단순 누적해 `do ... end`/`if`/loop block 종료 후 사라지는 local까지 계속 더했기 때문에 Chikan Train runtime handler를 212/449 locals로 과대계산했습니다. 보정 과정에서 Oracle 검토 지적을 반영해 `for` loop의 Lua compiler hidden control locals, colon method implicit `self`, vararg `...` 제외, branch/block active lifetime 회귀 테스트도 추가했습니다. 보정 후 실제 Chikan Train target을 `/tmp/opencode/chikan-active-local-check`로 재추출해 `lua/runtime/output.risulua`, `lua/runtime/button_click.risulua`, `docs/refactor-map.json` 등 module-table workspace와 `dist/Chikan_Train.risulua`가 모두 생성되고, plan/report가 `packable=true` / dist written 상태가 되는 것을 확인했습니다. Lua/wasmoon 200 local 제한을 보수적으로 막는 정책은 유지하되 기준은 누적 선언 수가 아니라 max active local 수입니다.

Boosted round-trip QA 메모: 2026-05-09 후속 hardening이 반영된 현재 코드로 `docs/bundle-mode/extract-test/targets/`의 Chikan Train charx, Merry RPG 모듈, Violated Girl 모듈을 `docs/bundle-mode/extract-test/boosted_bundling/`에 다시 extract → full-source recovery pack → recovery extract → recovered workspace repack했습니다. 세 케이스 모두 라운드트립 단계가 통과했고, `packages/core/src/cli/shared/lua-bundler/risulua-local-budget-analyzer.ts` built analyzer 기준 `first-extract`/`second-extract` dist 6개 모두 local-budget warning/error diagnostic이 0개였습니다. `roundtrip-local-budget-report.md`에 boosted 결과를 추가했으며, Chikan Train은 chunk max 5 / overall max 117, Merry RPG는 chunk max 22 / overall max 96, Violated Girl은 chunk max 32 / overall max 144로 집계됩니다. 기존 improved 결과 대비 Chikan Train 전체 최대는 120→117, Merry RPG wrapper-level peak는 106→96으로 낮아졌고, Violated Girl은 기존 hard-limit 해소 상태를 유지합니다. 추가 분석 결과 boosted 개선 폭이 제한적인 주된 이유는 현재 safe export alias rewrite가 인접한 `local Name = expr` + `M.Name = Name` 단일 패턴만 처리하고, dist에 많이 남는 `local function Name(...) ... end` + `M.Name = Name` FunctionDeclaration export 및 dependency alias local은 여전히 module source IIFE 안에 누적되기 때문입니다. 이 원인과 다음 개선 우선순위는 `roundtrip-local-budget-report.md`의 boosted 섹션에 반영했습니다.

개선안 분석 메모: 2026-05-09 boosted 개선 폭 한계를 추가 검토한 결과, 다음 구현 순서는 A) 비재귀/미사용/인접 `local function Name(...) ... end` + `M.Name = Name` export를 직접 `M.Name = function(...) ... end` 또는 `function M.Name(...) ... end`로 낮추기, B) bundler가 생성한 단일-use dependency alias local만 안전하게 inline/table assignment로 줄이기, C) A+B 측정 후에도 여유가 부족할 때에만 loader body flattening 또는 section splitting을 실험하기로 정리했습니다. Lua의 `local function`은 `local Name; Name = function(...) ... end` forward-binding semantics라서 함수 본문에 bare `Name` self-reference가 있거나 mutual recursion/local reuse가 있으면 직접 export rewrite는 금지해야 합니다. Merry RPG `button_actions.actions`가 `M.* = *` export alias 100개 이상으로 1차 효과 측정에 가장 적합하며, dependency alias 제거는 require/loader call count와 cached module identity가 바뀌지 않는 single-use generated alias부터 시작합니다.

1/2차 개선 구현 메모: 2026-05-09 RisuLua modular bundler에 보수적인 `local function` export rewrite와 single-use generated dependency alias inline을 적용했습니다. `local function Name(...) ... end` + `M.Name = Name`은 이전/함수 본문/후속 statement에 bare `Name` 참조가 없는 인접 패턴만 `function M.Name(...) ... end`로 낮추며, 재귀/self-reference, mutual recursion, export 뒤 local reuse는 보존합니다. require 교체 이후 `local __dep = __risulua_loaders["..."]()` generated alias는 단일 top-level direct value use이고 중간 statement가 진짜 빈 `local M = {}`뿐인 경우에만 inline해 delayed closure capture, non-empty module table side effect, multi-use alias, loader call count/order 위험을 제한했습니다. 신규 RED/GREEN 테스트와 blocking review 보강 테스트를 포함한 modular bundler 전체 41 tests, dist validator 21 tests, packages/core build를 통과했고, built bundler로 boosted fixture 3개를 재번들링한 결과 local-budget diagnostics 0개 및 remaining local-function export window 0개를 확인했습니다.

Round-trip QA 재갱신 메모: 2026-05-09 최신 bundler build로 `docs/bundle-mode/extract-test/boosted_bundling/`의 Chikan Train charx, Merry RPG 모듈, Violated Girl 모듈을 다시 extract → full-source recovery pack → recovery extract → recovered workspace repack했습니다. 3개 케이스 모두 라운드트립 단계가 통과했고, built `risulua-local-budget-analyzer` 기준 `first-extract`/`second-extract` dist 6개 모두 local-budget warning/error diagnostic이 0개였습니다. 최신 first/second extract 지표는 Chikan Train chunk max 5 / overall max 117, Merry RPG chunk max 22 / overall max 32, Violated Girl chunk max 32 / overall max 144이며, `docs/bundle-mode/extract-test/improved_bundling/roundtrip-local-budget-report.md`의 boosted 섹션을 이 수치로 갱신했습니다. Merry RPG wrapper-level peak가 106 → 96 → 32로 낮아져 현재 측정값 기준 3차 loader body flattening/section splitting은 필수 구현이 아니라 별도 architectural plan 평가 대상으로 남겨둡니다.

Round-trip 보고서 비교축 보정: 2026-05-09 `roundtrip-local-budget-report.md`의 핵심 비교를 기준 지표인 Legacy 원본 Lua → 기존 `output/` dist → 개선1 `improved_bundling` → 개선2 `boosted_bundling` 축으로 재정리했습니다. 메인 비교표와 케이스별 표, 로컬 변수 사용량 상위 함수 표에 모두 Legacy 기준 컬럼을 복구했고, 테이블 직후에 `청크 최대`가 파일 최상위 chunk scope에서 동시에 살아 있는 활성 local 수의 최대값이며 wasmoon/Lua 200 local hard limit을 판단하는 핵심 top-level budget 지표라는 설명을 추가했습니다.

### Remaining

#### RisuLua Wasmoon Local Variable Limit

- [x] `docs/wasmoon-var-limit.md`에 wasmoon/Lua local 200개 제한, 현재 modular bundler의 top-level loader local 폭증 문제, 실제 고위험 fixture(`RPG_Violated_Girl.risulua`)와 table-backed loader registry 해결안을 정리하기.
- [x] `docs/superpowers/plans/2026-05-09-risulua-wasmoon-local-budget-bundler.md`에 table-backed loader registry 실행 계획 보강하기. 테스트 수정 범위, `RPG_Violated_Girl` fixture 회귀, 150/180/190 warning 및 200 hard-limit error 정책, analyzer scope traversal 규격, 최종 focused verification 범위를 반영함.
- [x] RisuLua modular bundler를 table-backed loader registry로 전환하고 generated dist local-budget analyzer 및 validation warning/error 정책 구현하기. `__risulua_loaders["module.id"]()` runtime lookup, `generatedLoaders` 호환성 유지, 150/180/190 warning 및 200 hard-limit error 검증을 focused tests/build로 확인함.
- [x] `docs/superpowers/plans/2026-05-09-risulua-module-local-budget-hardening.md` 계획에 따라 scope/peak local-budget diagnostics를 보강하고, 안전한 export alias(`local Name = async(function...)` 직후 `M.Name = Name`)를 직접 table assignment로 낮춰 module wrapper/IIFE local pressure를 줄이기.
- [x] 1차: boosted 분석 결과에 따라 비재귀/미사용/인접 `local function Name(...) ... end` 직후 `M.Name = Name` export 패턴만 직접 table function assignment로 낮추는 rewrite를 설계/구현하기. 함수 본문 bare `Name` self-reference, mutual recursion, export 뒤 local reuse, source-map/navigation 영향은 반드시 회귀 테스트로 검증하고, 재귀 local function은 기존처럼 보존한다.
- [x] 2차: module source IIFE 안에 누적되는 bundler-generated dependency alias local 중 single-use alias부터 safe inline require/loader call 또는 직접 table assignment로 줄일 수 있는지 설계하고 Merry RPG boosted fixture로 local-budget 효과와 require/loader call count 보존을 검증하기. multi-use alias, identity-sensitive alias, hot path alias는 기본 보존한다.
- [ ] 3차: 1차/2차 측정 후에도 local-budget headroom이 부족한 경우에만 wrapper/IIFE 구조 자체(`local __risulua_module_result = (function() ... end)()`)를 유지할지, loader body flattening 또는 section splitting으로 function prototype 경계를 나눌지 별도 architectural plan으로 평가하기. return/nil-cache/circular dependency/early return/upvalue semantics를 검증 범위에 포함한다.

#### RisuLua Split Navigation Metadata

- [x] module-table 생성 산출물에 sourceRange 기반 navigation metadata를 추가하기. MVP는 생성된 `.risulua` 함수/액션 블록 앞의 `-- Generated from <source>:<line>` 및 선택적 LuaLS `---@source <path>:<line>:<char>` 주석, 그리고 `docs/risulua-button-action-index.json` 별도 sidecar로 구성한다. `risu-trigger="name"` / `{{button::...::name}}` 사용처는 action name별로 collision-aware 그룹화하고, export manifest에는 button usage를 섞지 않는다.

#### RisuLua Source Comment Definition

- [x] generated `.risulua` `---@source <path>:<line>:<column>` comments가 cbs-lsp `textDocument/definition` routing을 통해 Go to Definition으로 원본 artifact 위치에 이동하도록 구현하기. Subagent-driven 계획은 `docs/superpowers/plans/2026-05-08-risulua-source-comment-definition.md`를 따른다.
- [x] `.risulua` 기본 association을 `lua`로 유지해 native LuaLS 기능을 보존하면서, native definition aggregation 지연은 `---@source` path DocumentLink command로 우회하는 client-side 즉시 이동 UX를 추가하기.

#### RisuLua Generated Bridge Navigation

- [x] Rust/WASM Lua analyzer v1에 generated bridge indexing 필드를 추가하고, cbs-lsp definition/hover fast-path에서 `publicName = requireAlias.memberName` bridge를 module member definition과 원본 `---@source` metadata로 연결하기. Subagent-driven 계획은 `docs/superpowers/plans/2026-05-09-risulua-bridge-indexing.md`를 따른다.

#### RisuLua Embedded Recovery Manifest

- [x] packed `.charx`/`.risum`에 `lua/`, `docs/`, `legacy/`가 포함되지 않는 문제를 해결하기 위해 optional `--risulua-recovery full-source`를 구현한다. Subagent-driven 계획은 `docs/superpowers/plans/2026-05-09-risulua-embedded-recovery-manifest.md`를 따른다.

#### Backlog

- [ ] CBS LSP full suite의 fragment-routing/workspace-state/release fixture 관련 기존 실패 24건을 별도 조사하기. 이번 LuaLS sidecar surface targeted tests와 build는 통과했지만 `npm run --workspace cbs-language-server test` 전체 실행에서는 Lua string fragment mapping 기대값, LuaLS workspace sync, workspace variable flow, activation CodeLens, watched-file refresh, `.changeset`/GitHub workflow fixture 기대값이 현재 작업트리와 맞지 않는 실패가 남아 있음
- [ ] 외부 archive fixture(`risulua-static-clean-refactor-review/examples/*.risulua`)가 없는 환경에서 `risulua-split-module-table-archive-report.test.ts`가 skip되는 상태를 별도 CI fixture 정책으로 정리하기. 이번 navigation metadata 검증에서는 fixture 존재 여부 가드를 추가해 `rtk npm run --workspace packages/core test -- risulua-split-module-table`가 134 passed / 2 skipped로 완료됨
- [ ] `tests/custom-extension-diagnostics.test.ts`의 `maps lua full file to single fragment` 기대값을 현재 Rust/WASM Lua string fragment mapping(`lua-string:1`) 정책에 맞게 재판정하기. 이번 definition targeted 검증 중 전체 custom-extension diagnostics 실행에서 기존 기대값 `full`과 현재 구현값이 불일치하는 것을 확인했으며, 변경한 availability snapshot 케이스는 별도 targeted test로 통과함
- [ ] `.risuvar` default-only 변수 보강 검증 중 `tests/lsp-server-integration.test.ts`에 남아 있는 기존 server seam 실패 15건을 별도 조사하기. Provider/service focused tests와 build는 통과했지만 required targeted command에서는 LuaLS workspace sync/overlay, workspace symbol, cross-file references/rename/hover/completion, CodeLens, watched-file refresh, diagnostics refresh, incremental rebuild 관련 기존 기대값 불일치가 계속 남아 있음
- [ ] `rtk npm run --workspace risu-workbench-core test` 전체 suite의 `tests/domain-phase1-extraction.test.ts > domain purity guard > keeps Node.js imports out of src/domain` 실패를 별도 조사하기. `src/domain/lua-wasm/loader.ts`에서 `node:module` / `createRequire`를 import해 domain purity guard가 reject하고 있으며, 이는 이번 replaceGlobalNote migration과 무관한 기존 unrelated failure임
- [ ] `rtk npm run --workspace risu-workbench-core test` 전체 suite의 `tests/export-surface.test.ts > export surface (snapshot) > root entry exports match snapshot` 실패를 별도 조사하기. 루트 export snapshot이 `CBS_ARTIFACT_EXTENSIONS`, Lua WASM analyzer/runtime export, `mapTextToCbsFragments` 등 현재 export key와 불일치해 stale snapshot으로 판단되며, 이번 replaceGlobalNote migration과 무관한 기존 unrelated failure임
